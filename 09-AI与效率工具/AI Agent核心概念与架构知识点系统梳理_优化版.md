---
title: AI Agent 核心概念与架构知识点系统梳理
tags: [AI与效率工具, AIAgent, Agent, 大模型, 自主智能体, 架构, 面试]
created: 2026-08-13
updated: 2026-08-13
---

# AI Agent 核心概念与架构知识点系统梳理（优化版）

> **文档说明**：系统梳理 AI Agent（智能体）的核心概念、架构模式、关键能力（规划/记忆/工具调用）、Multi-Agent 协作与应用场景。

---

## 1. 概述

AI Agent（人工智能智能体）是能够**自主感知环境、做出决策并执行行动**以完成目标的 AI 系统。与传统 LLM 只能"对话"不同，Agent 能调用工具、访问外部信息、执行多步骤任务，是 LLM 从"聊天机器人"向"智能助手"演进的关键形态。

**Agent vs 普通 LLM 对话**：

| 维度 | 普通 LLM 对话 | AI Agent |
|------|--------------|----------|
| 能力 | 文本生成 | 文本 + 工具调用 + 自主决策 |
| 任务 | 单轮问答 | 多步骤复杂任务 |
| 外部交互 | 无 | 搜索、代码执行、API 调用 |
| 自主性 | 被动回答 | 主动规划和执行 |
| 记忆 | 对话历史 | 短期 + 长期记忆 |

---

## 2. Agent 核心架构

### 2.1 感知-决策-执行循环

```
┌─────────────────────────────────────┐
│           AI Agent 循环              │
│                                     │
│  感知（Perception）                 │
│    ↓ 接收用户目标、环境信息          │
│  规划（Planning）                   │
│    ↓ 拆解任务、制定步骤              │
│  决策（Decision）                   │
│    ↓ 选择行动/工具                   │
│  执行（Action）                     │
│    ↓ 调用工具、执行代码              │
│  观察（Observation）                │
│    ↓ 获取执行结果                    │
│  反思（Reflection）                 │
│    ↓ 评估结果，调整策略              │
│    ↓ 循环直到任务完成                │
└─────────────────────────────────────┘
```

### 2.2 三大核心组件

#### 1. 规划（Planning）

将复杂目标拆解为可执行的子任务。

- **任务分解**：LLM 将大目标拆为小步骤
- **反思与修正**：根据执行结果调整计划
- **树状规划**：探索多条路径，选择最优

#### 2. 记忆（Memory）

- **短期记忆**：当前对话上下文（Context Window）
- **长期记忆**：外部存储（向量数据库），持久化知识
- **工作记忆**：Agent 当前任务的状态和中间结果

#### 3. 工具调用（Tool Use / Function Calling）

Agent 调用外部工具扩展能力：
- 搜索引擎（Google/Bing）
- 代码执行（Python REPL）
- API 调用（天气、数据库、第三方服务）
- 文件读写
- 浏览器操作

> 🔍 **知识点深度解析**
>
> **作用**：理解 Agent 的三大核心组件是构建 Agent 系统的基础。
>
> **原理**：Agent 的本质是 LLM + 工具 + 循环。LLM 作为"大脑"负责理解和决策，工具作为"手脚"扩展能力，循环让 Agent 能自主完成多步骤任务。规划能力来自 LLM 的推理能力（CoT/ToT），将复杂目标拆解。记忆分为短期（上下文窗口，有限）和长期（向量数据库，通过 RAG 检索），解决 LLM 上下文有限和无持久化的问题。工具调用通过 Function Calling 实现：LLM 输出结构化的工具调用指令（JSON），系统执行后将结果返回给 LLM，LLM 根据结果决定下一步。ReAct 模式将推理（Reasoning）和行动（Acting）交替进行，是 Agent 的经典范式。
>
> **用法要点**：① Agent 适合多步骤、需要外部信息、需要执行操作的任务；② 简单问答不需要 Agent，直接用 LLM 更高效；③ 工具描述要清晰准确，LLM 才能正确选择和使用；④ 面试常考：Agent 定义、三大组件、ReAct 模式、规划/记忆/工具调用原理、Agent vs LLM。

---

## 3. 经典 Agent 范式

### 3.1 ReAct（Reasoning + Acting）

```
问题：北京今天适合出门吗？
思考：我需要知道北京今天的天气，应该调用天气查询工具。
行动：weather_api("北京")
观察：北京今天晴，25-32度，空气质量良，无降水。
思考：天气晴好，温度适宜，适合出门。需要提醒防晒。
回答：北京今天晴，25-32度，空气质量良，适合出门，注意防晒。
```

**特点**：推理和行动交替，每步思考后执行一个动作，根据观察结果继续推理。

### 3.2 Plan-and-Execute

```
1. 规划阶段：一次性制定完整计划
   计划：[搜索资料 → 整理大纲 → 撰写文章 → 校对润色]
2. 执行阶段：按计划逐步执行
3. 反思阶段：检查结果，必要时重新规划
```

**特点**：先规划后执行，适合目标明确的复杂任务。

### 3.3 Reflexion（反思型 Agent）

- 执行任务后进行自我反思
- 将反思结果存入记忆
- 下次类似任务时参考历史经验
- 持续迭代提升表现

---

## 4. 工具调用（Function Calling）

### 4.1 原理

LLM 输出结构化的工具调用请求，系统执行后返回结果。

```python
# 工具定义
tools = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "查询指定城市的天气",
            "parameters": {
                "type": "object",
                "properties": {
                    "city": {"type": "string", "description": "城市名称"}
                },
                "required": ["city"]
            }
        }
    }
]

# LLM 返回工具调用
{
    "tool_calls": [{
        "function": {
            "name": "get_weather",
            "arguments": "{\"city\": \"北京\"}"
        }
    }]
}

# 执行工具，将结果返回给 LLM
```

### 4.2 常用工具类型

| 工具类型 | 示例 | 用途 |
|----------|------|------|
| 搜索 | Google Search、Tavily | 获取实时信息 |
| 代码执行 | Python REPL、Jupyter | 计算、数据处理 |
| API 调用 | 天气、股票、数据库 | 获取结构化数据 |
| 文件操作 | 读写文件 | 文档处理 |
| 浏览器 | Playwright、Selenium | 网页操作 |
| 数据库 | SQL 查询 | 数据检索 |
| 向量检索 | RAG | 知识库问答 |

---

## 5. 记忆系统

### 5.1 记忆层级

```
感觉记忆（瞬时）→ 短期记忆（上下文）→ 长期记忆（向量库）
```

### 5.2 长期记忆实现

- **向量数据库**：Pinecone、Chroma、Milvus、Weaviate
- **存储内容**：对话历史、用户偏好、知识库文档、执行经验
- **检索方式**：语义相似度检索（RAG）
- **记忆管理**：重要性评分、遗忘机制、摘要压缩

### 5.3 RAG（检索增强生成）

```
用户问题 → 向量化 → 向量库检索相关文档 → 拼接上下文 → LLM 生成回答
```

RAG 是 Agent 长期记忆和知识增强的核心技术，解决 LLM 知识过时和幻觉问题。

---

## 6. Multi-Agent（多智能体）

### 6.1 概念

多个 Agent 协作完成复杂任务，每个 Agent 有专门角色和能力。

### 6.2 协作模式

| 模式 | 说明 | 示例 |
|------|------|------|
| **顺序协作** | Agent A 输出 → Agent B 输入 | 研究员→撰稿人→编辑 |
| **并行协作** | 多个 Agent 同时工作，结果汇总 | 多搜索引擎并行查询 |
| **辩论模式** | Agent 之间讨论，达成共识 | 多模型辩论提升准确性 |
| **层级管理** | Manager Agent 分配任务给 Worker | 项目经理→开发者 |
| **自主协作** | Agent 自主决定何时与谁协作 | AutoGen 群聊 |

### 6.3 典型架构（CrewAI）

```
项目经理（规划）→ 研究员（搜索）→ 撰稿人（写作）→ 编辑（校对）
```

---

## 7. Agent 应用场景

| 场景 | 说明 | 代表产品 |
|------|------|----------|
| **编程助手** | 自主写代码、调试、运行测试 | Cursor Agent、Devin |
| **研究助手** | 自动搜索、整理、撰写报告 | Perplexity、GPTs |
| **数据分析** | 自动查询数据、生成图表、分析 | ChatGPT Code Interpreter |
| **客服 Agent** | 自动处理工单、查询订单 | 企业客服系统 |
| **内容创作** | 自动调研、写作、配图 | 多 Agent 协作 |
| **个人助理** | 日程管理、邮件处理、任务执行 | 个人 AI 助理 |
| **自动化工作流** | 连接多工具，自动执行流程 | Dify、n8n + AI |

---

## 8. Agent 的挑战

- **可靠性**：Agent 可能在循环中卡住或偏离目标
- **成本**：多轮 LLM 调用，Token 消耗大
- **延迟**：多步骤执行，响应时间长
- **安全性**：工具调用可能执行危险操作，需权限控制
- **评估**：Agent 行为复杂，难以量化评估
- **幻觉**：LLM 幻觉会导致 Agent 做出错误决策

---

## 8.1 Agent 类型分类

| 类型 | 特点 | 示例 |
|------|------|------|
| **反射型** | 直接根据感知行动，无内部状态 | 简单规则机器人 |
| **基于模型** | 维护内部状态模型 | 有记忆的助手 |
| **基于目标** | 以目标为导向规划行动 | 任务执行 Agent |
| **基于效用** | 评估多个目标的效用，选最优 | 多目标决策 Agent |
| **理性 Agent** | 追求期望效用最大化 | 理论最优模型 |

---

## 8.2 规划算法

- **HTN**（层次任务网络）：将任务递归分解为子任务
- **MCTS**（蒙特卡洛树搜索）：搜索决策树，选最优行动
- **ToT**（思维树）：LLM 探索多条推理路径，评估后选择
- **规划与执行分离**：Planner 制定计划，Executor 执行

---

## 8.3 记忆类型详解

| 记忆类型 | 内容 | 存储 |
|----------|------|------|
| **情景记忆**（Episodic） | 具体事件、对话历史 | 向量数据库 |
| **语义记忆**（Semantic） | 事实知识、概念 | 知识库/RAG |
| **程序性记忆**（Procedural） | 技能、操作流程 | 工具/代码 |
| **工作记忆**（Working） | 当前任务状态、中间结果 | 上下文窗口 |

---

## 8.4 MCP（Model Context Protocol）

Anthropic 提出的工具调用标准化协议：
- **Server/Client 架构**：工具以 MCP Server 形式提供
- **标准化接入**：任何支持 MCP 的 Agent 都能使用
- **资源/工具/提示词**三种暴露类型
- 目标：解决工具接入碎片化问题

---

## 8.5 Agent 评估基准

| 基准 | 评测内容 |
|------|----------|
| **AgentBench** | 多环境 Agent 能力（8个环境） |
| **WebArena** | 真实网页操作任务 |
| **SWE-bench** | 真实 GitHub Issue 修复 |
| **GAIA** | 通用 AI 助手任务（需多步推理+工具） |

---

## 8.6 Agent 安全与对齐

- **工具滥用**：限制工具权限，敏感操作需人工确认
- **权限边界**：最小权限原则，沙箱执行
- **目标漂移**（Goal Misgeneralization）：Agent 偏离原始目标
- **可中断性**：随时可停止，避免无限循环
- **红队测试**：模拟攻击测试 Agent 安全性

---

## 9. 面试高频考点

1. **AI Agent 定义**：自主感知-决策-执行的 AI 系统
2. **三大核心组件**：规划、记忆、工具调用
3. **ReAct 模式**：推理+行动交替，Agent 经典范式
4. **Function Calling**：原理、工具定义、执行流程
5. **记忆系统**：短期/长期记忆、向量数据库、RAG
6. **Multi-Agent**：协作模式、典型架构
7. **Agent vs LLM**：区别、适用场景
8. **Plan-and-Execute**：先规划后执行范式
9. **Agent 挑战**：可靠性、成本、延迟、安全
10. **RAG**：检索增强生成，Agent 知识增强核心
11. **Agent 类型**：反射型/基于模型/基于目标/基于效用
12. **规划算法**：HTN/MCTS/ToT，规划与执行分离
13. **记忆类型**：情景/语义/程序性/工作记忆
14. **MCP 协议**：Model Context Protocol，标准化工具接入
15. **Agent 评估**：AgentBench/WebArena/SWE-bench/GAIA
16. **Agent 安全**：工具滥用、权限边界、目标漂移、可中断性

---

## 📝 精简总结

- AI Agent = LLM（大脑）+ 工具（手脚）+ 循环（自主执行）
- 三大组件：规划（任务拆解）、记忆（短期上下文+长期向量库）、工具调用（Function Calling）
- Agent 类型：反射型→基于模型→基于目标→基于效用→理性 Agent（演进路径）
- 规划算法：HTN层次分解、MCTS蒙特卡洛树搜索、ToT思维树、规划与执行分离
- 记忆类型：情景记忆（事件）、语义记忆（知识）、程序性记忆（技能）、工作记忆（当前状态）
- 经典范式：ReAct（推理行动交替）、Plan-and-Execute（先规划后执行）、Reflexion（反思迭代）
- 工具调用：LLM 输出结构化 JSON，系统执行后返回结果
- MCP 协议：Anthropic提出，标准化工具接入，Server/Client架构，解决碎片化
- 记忆：RAG 检索增强，向量数据库存储长期记忆
- Multi-Agent：多智能体协作，顺序/并行/辩论/层级模式
- 评估基准：AgentBench（多环境）、WebArena（网页操作）、SWE-bench（代码修复）、GAIA（通用助手）
- 安全：工具滥用防护、最小权限、目标漂移监控、可中断性、红队测试
- 应用：编程助手、研究助手、数据分析、客服、内容创作、个人助理
- 挑战：可靠性、成本、延迟、安全、评估、幻觉

---

[[09-AI与效率工具/MOC-AI与效率工具|← 返回 AI 与效率工具 MOC]] | [[Home|🏠 返回首页]]
