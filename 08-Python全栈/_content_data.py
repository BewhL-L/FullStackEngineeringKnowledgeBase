# -*- coding: utf-8 -*-
# 每个知识点深度解析内容：{ 文件名: { 标题文本: (作用, 原理, [要点...]) } }
CONTENT = {

"Python Web开发框架知识点系统梳理_优化版.md": {
"2.1 核心架构（MTV）": (
"Django 采用 MTV（Model-Template-View）架构将数据处理、展示与业务逻辑解耦，使团队可以分层开发、独立维护。",
"请求经 URLconf 路由到 View（业务逻辑层），View 操作 Model（ORM 读写数据库）并选择 Template 渲染；MTV 是 MVC 的变体——Django 的 View 实际对应 MVC 的 Controller，Template 对应 MVC 的 View。",
["Model 用 ORM 描述数据结构与表关系", "View 处理请求并返回响应（FBV 函数或 CBV 类）", "Template 负责 HTML 渲染与展示", "URLconf 将 URL 映射到对应 View", "面试常考 MTV 与 MVC 的对应关系"]),

"2.2 项目结构": (
"规范的项目结构区分全局配置与功能应用，是多人协作与长期可维护的基础。",
"Django 项目（project）包含全局配置（settings/urls/wsgi），应用（app）是独立功能模块（models/views/urls/admin 等），通过 INSTALLED_APPS 注册；manage.py 是命令行统一入口。",
["一个 project 可包含多个 app", "settings.py 集中配置数据库/中间件/模板等", "urls.py 分根路由与应用路由两级", "migrations 保存数据库变更历史", "新增功能优先建独立 app 而非堆在单一模块"]),

"2.3 ORM（核心）": (
"ORM 让开发者用 Python 类和对象操作数据库，避免手写 SQL，显著提升开发效率与代码可移植性。",
"模型类映射到数据表、字段映射到列；QuerySet 是惰性序列，只有在迭代/切片/求值（list、len、bool）时才真正执行 SQL；select_related 用 SQL JOIN 预取一对一/多对一，prefetch_related 用额外查询+Python 关联解决多对多/反向外键的 N+1 问题。",
["用 select_related/prefetch_related 消除 N+1", "values()/values_list() 返回轻量字典/元组", "大数据量用 iterator() 防止内存溢出", "filter/get/exclude/order_by 为高频 API", "联表查询优先预加载关联对象"]),

"2.4 视图与路由": (
"视图处理请求并返回响应，路由将 URL 映射到底层视图函数或类，是 Web 框架的入口枢纽。",
"FBV 是普通函数接收 request 返回 response；CBV 继承 View 并通过 as_view() 分发 get/post 等方法；urls.py 的 path()/re_path() 用转换器（<int:pk>）提取路径参数。",
["用 @app/path 装饰器或 path() 绑定 URL 与方法", "request.args/get_json() 取参", "jsonify 返回 JSON", "生产禁用 debug（代码泄露/任意执行风险）", "生产用 gunicorn/uwsgi 启动"]),

"3.1 核心特点": (
"理解 Flask 的定位（轻量微框架）有助于在合适场景选型并设计合理架构。",
"Flask 核心只提供路由与请求处理，WSGI 层基于 Werkzeug，模板基于 Jinja2，其余功能靠第三方扩展；所谓“微”指核心小而非能力弱。",
["微框架=核心精简，功能靠扩展", "基于 Werkzeug（WSGI）+ Jinja2（模板）", "适合小型项目/API/微服务/原型", "灵活度高但需自己组装组件", "与 Django 最大区别是“内置电池” vs “按需扩展”"]),

"3.2 基本使用": (
"掌握 Flask 最小应用写法与路由装饰器是入门与写 API 的关键。",
"Flask(__name__) 创建应用，@app.route 注册路由并绑定视图函数；请求对象 request 提供参数/表单/JSON；jsonify 将 dict 序列化为 JSON 响应；app.run(debug=True) 仅用于开发。",
["用 @app.route 装饰器绑定 URL 与方法", "request 提供参数/表单/JSON 访问", "jsonify 返回标准 JSON 响应", "生产环境禁用 debug 模式", "生产使用 gunicorn/uwsgi 启动"]),

"3.3 蓝图（Blueprint）": (
"蓝图将路由与视图分组模块化，便于大型项目拆分、复用与测试。",
"Blueprint 在子模块中定义路由，再 register_blueprint 挂载到 app；可设 url_prefix 统一前缀；配合应用工厂模式可创建多个 app 实例（测试/多环境）。",
["用 Blueprint 拆分模块路由", "url_prefix 统一模块前缀", "配合工厂模式支持多实例", "蓝图间可嵌套组合", "适合中大型 Flask 项目组织"]),

"4.1 核心特点": (
"明确 FastAPI 的差异化优势（异步+类型+自动文档）是选型与面试重点。",
"基于 Starlette（ASGI）提供原生 async，Pydantic 做数据校验/序列化，类型提示驱动参数解析与文档生成；性能接近 Node/Go。",
["ASGI 异步、并发能力高", "Pydantic 类型校验与序列化", "自动生成 Swagger/ReDoc 文档", "类型提示即接口文档", "适合高性能 API 与异步场景"]),

"4.2 基本使用": (
"掌握 FastAPI 路由、Pydantic 模型与响应模型是开发 API 的基础。",
"用 @app.get/post 装饰器声明路径操作，函数参数按类型区分路径/查询/请求体；Pydantic 模型定义入参与出参，response_model 控制返回字段；async def 支持异步处理。",
["用 Pydantic 模型定义请求/响应", "response_model 过滤返回字段", "路径/查询/请求体由类型自动解析", "异步函数用 async def", "HTTPException 返回标准错误"]),

"4.3 依赖注入": (
"依赖注入实现可复用、可测试的共享逻辑（数据库会话、鉴权、公共参数）。",
"Depends 声明依赖，FastAPI 在调用路径操作前解析依赖树并注入；依赖可嵌套，yield 形式的依赖能自动管理资源（如 db 会话关闭）。",
["Depends 注入依赖对象", "用 yield 管理资源生命周期", "依赖可嵌套复用", "常用于数据库会话与鉴权", "同一依赖默认每次请求计算一次（可缓存）"]),

"4.5.1 序列化器（Serializer）": (
"DRF 的 Serializer 负责复杂数据与 JSON 的双向转换与校验，是 API 数据层核心。",
"Serializer 将模型实例/QuerySet 序列化为 JSON，也能将输入反序列化为校验后的数据；ModelSerializer 按模型自动生成字段；SerializerMethodField 自定义只读字段；validate_<field> 做字段级校验。",
["ModelSerializer 自动生成字段", "SerializerMethodField 自定义只读字段", "validate_xxx 自定义字段校验", "read_only/write_only 控制字段方向", "注意嵌套序列化器的 N+1 与性能"]),

"4.5.2 视图集与路由": (
"ViewSet+Router 把 CRUD 操作与 URL 自动绑定，显著减少样板代码。",
"ViewSet 将 list/retrieve/create/update/destroy 等动作封装为一个类；DefaultRouter 根据 ViewSet 自动生成 RESTful 路由；@action 添加自定义动作，detail=True 操作单条资源。",
["ViewSet 聚合 CRUD 动作", "Router 自动生成 URL", "@action 添加自定义动作", "权限/过滤/分页在 ViewSet 统一配置", "相比 APIView 更简洁"]),

"三框架中间件对比": (
"中间件统一在请求/响应前后插入横切逻辑（日志、鉴权、CORS、限流），是框架的重要扩展点。",
"Django 中间件是含 __call__/process_* 的类，按 MIDDLEWARE 顺序形成处理链；Flask 用 before/after_request 钩子；FastAPI 用 @app.middleware 装饰器（基于 Starlette），三种机制本质都是“请求→视图→响应”的钩子。",
["中间件作用于请求前与响应后", "Django 用类+列表顺序控制", "Flask 用 before/after_request 钩子", "FastAPI 用装饰器声明", "常见用途：日志、CORS、鉴权、限流、计时"]),
},

"Python中间件与异步任务知识点系统梳理_优化版.md": {
"2.1 基本配置": (
"Celery 的基本配置是连接 Broker 与 Result Backend、定义序列化与并发参数的起点。",
"Celery 通过 Celery(app) 或独立实例创建应用，配置以 broker_url、result_backend 指定消息代理与结果存储；配置可写在代码、配置文件或 Django settings 中。",
["broker_url 指定消息中间件（Redis/RabbitMQ）", "result_backend 指定结果存储", "task_serializer 控制序列化格式", "配置可集中管理便于环境切换", "Django 项目常用 celery.py + settings 集成"]),

"2.2 定义与调用任务": (
"任务是 Celery 的调度单元，掌握定义与异步调用是使用该框架的基础。",
"用 @app.task 装饰函数即定义为任务；.delay()/.apply_async() 将任务推入 Broker 异步执行，调用方立即返回 AsyncResult 而非等待结果。",
["@app.task 装饰器定义任务", "delay()/apply_async() 异步触发", "调用方立即返回不阻塞", "AsyncResult 可查询任务状态与结果", "耗时操作应剥离为异步任务"]),

"2.3 定时任务（Celery Beat）": (
"Celery Beat 提供类 cron 的周期性任务调度，适合报表、清理、同步等定时作业。",
"Beat 是独立调度进程，按 schedule 配置周期性将任务发往 Broker；支持 interval（间隔）与 crontab（类 cron 表达式）两种触发方式。",
["Beat 是独立调度进程", "interval 表示固定间隔", "crontab 支持类 cron 表达式", "调度配置与任务定义分离", "需同时运行 beat 与 worker"]),

"2.4 Worker 启动": (
"Worker 是真正执行任务的进程，启动参数直接影响并发度与资源占用。",
"celery -A proj worker 启动消费进程；-c/--concurrency 控制并发数（默认等于 CPU 数），可配合 --autoscale 弹性扩缩；事件循环类型取决于 Broker（Redis 用 prefork/thread）。",
["-A 指定 Celery 应用", "-c 控制并发 worker 数", "--autoscale 弹性扩缩容", "不同 Broker 支持不同池类型", "生产建议后台守护运行"]),

"2.6 任务优先级与路由": (
"优先级与路由让不同任务进入不同队列，实现资源隔离与重要任务优先处理。",
"通过 task_routes 或 queue 参数将任务绑定到指定队列，Worker 用 -Q 指定消费哪些队列；优先级依赖 Broker 支持（如 RabbitMQ 的 priority）。",
["task_routes 按名称路由任务", "Worker -Q 指定消费队列", "不同队列实现资源隔离", "优先级依赖 Broker 能力", "关键任务应单独队列避免阻塞"]),

"3.1 Redis 作为 Broker": (
"Redis 是最常用的轻量级 Broker，部署简单、性能高，适合多数中小规模场景。",
"任务以列表/流结构存入 Redis，Worker 用 BRPOP 等方式阻塞取任务；Redis 内存存储，重启可能丢失未消费任务（可开启 AOF/RDB 持久化缓解）。",
["部署简单、性能优秀", "基于内存，需注意持久化", "支持阻塞弹出高效消费", "不适合超大规模消息堆积", "常配合 Redis 同时做缓存与 Broker"]),

"3.2 RabbitMQ 作为 Broker": (
"RabbitMQ 是功能完备的消息中间件，适合对可靠性、路由和吞吐要求高的场景。",
"基于 AMQP 协议，支持交换机（Exchange）灵活路由、消息确认（ack）、持久化与优先级队列；相比 Redis 更擅长复杂的消息可靠投递。",
["基于 AMQP，路由能力强", "支持消息确认与持久化", "支持优先级队列", "可靠性高于 Redis", "运维复杂度也更高"]),

"3.3 Kafka": (
"Kafka 是高吞吐分布式日志系统，适合海量事件流与削峰填谷。",
"以分区（partition）日志方式存储消息，支持多消费者组、持久化与水平扩展；Celery 对 Kafka 支持有限，通常直接用 kafka-python 消费。",
["高吞吐、可水平扩展", "分区日志持久化", "适合事件流与大数据管道", "Celery 原生支持弱", "常用于日志/埋点/流处理"]),

"4.1 Django Q / django-celery": (
"Django Q 是轻量异步任务方案，原生集成 Django 且无需独立 Broker 即可用。",
"基于 Django ORM 作 Broker，配套 Django Admin 管理任务与调度；相比 Celery 更轻，适合已用 Django、任务量不大的项目。",
["轻量、与 Django 深度集成", "可用 ORM 作 Broker", "自带 Admin 任务管理", "适合中小项目", "生态与扩展性弱于 Celery"]),

"4.2 RQ（Redis Queue）": (
"RQ 是基于 Redis 的极简任务队列，API 直观、学习成本低。",
"用 Queue 将函数入队，独立 worker 进程消费；仅依赖 Redis，无复杂配置，适合简单后台任务。",
["仅依赖 Redis，极简", "API 直观易上手", "适合简单后台任务", "功能比 Celery 少", "无内置复杂定时调度"]),

"4.3 Dramatiq": (
"Dramatiq 是现代化的可靠任务处理库，强调简洁 API 与中间件机制。",
"任务用 @dramatiq.actor 声明，配套 broker（RabbitMQ/Redis）与 worker；通过中间件实现重试、限流、监控，支持任务结果存储。",
["API 简洁、中间件化", "支持 RabbitMQ/Redis", "内置重试与限流", "比 Celery 年轻、生态较小", "适合追求简洁的异步任务"]),

"4.4 asyncio 原生异步": (
"asyncio 是 Python 原生并发模型，适合 IO 密集型协程任务而无需外部 Broker。",
"基于事件循环与协程（async/await），单线程内通过挂起/恢复实现高并发；适合爬虫、并发请求等场景，但不适合 CPU 密集任务。",
["基于事件循环与协程", "适合 IO 密集型并发", "单线程避免线程开销", "不适合 CPU 密集", "可与任务队列互补"]),

"5.1 发布订阅模式": (
"发布订阅解耦消息生产者与消费者，支持一对多广播与动态扩展订阅者。",
"发布者将消息发到主题（topic/channel），所有订阅者各自收到副本；Redis 的 pub/sub、Kafka 的消费者组都是典型实现。",
["生产者与消费者解耦", "支持一对多广播", "订阅者可动态增减", "消息通常不持久（视实现）", "适合事件通知/实时推送"]),

"5.2 Webhook": (
"Webhook 是反向 API，由事件触发主动向预设 URL 推送通知，实现系统间实时集成。",
"当源系统发生事件时主动 POST 数据到目标 URL；相比轮询更实时、省资源，但需目标方提供可公网访问的稳定接口并处理重试与签名校验。",
["事件驱动，主动回调", "比轮询更实时省资源", "需接收方提供稳定 URL", "需校验签名防伪造", "要做好重试与幂等"]),
},

"Python全栈前端集成知识点系统梳理_优化版.md": {
"2.1 Jinja2": (
"Jinja2 是 Flask 默认的模板引擎，负责把后端数据渲染进 HTML，实现服务端页面。",
"模板中通过 {{ }} 输出变量、{% %} 写控制结构；引擎在渲染时把上下文变量填充进模板，生成最终 HTML 返回浏览器。",
["{{ }} 输出变量", "{% %} 写逻辑控制", "支持模板继承 base.html", "自动转义防 XSS", "是 Flask/Django 之外通用引擎"]),

"2.2 Jinja2 核心语法": (
"掌握 Jinja2 的变量、控制流与继承语法是写可维护模板的关键。",
"支持 for/if 等控制结构、宏（macro）复用片段、include/extends 做模板组合；过滤器（|）对变量做格式化处理。",
["for/if 控制结构", "extends/include 模板继承", "macro 定义可复用片段", "过滤器 | 格式化变量", "block 实现可覆盖区域"]),

"2.3 自定义过滤器": (
"自定义过滤器封装可复用的展示逻辑，避免模板中写复杂表达式。",
"用 @app.template_filter 或 env.filters 注册函数，模板中通过 {{ var|filter }} 调用；适合格式化日期、截断文本、转换状态等。",
["用装饰器或 filters 字典注册", "模板中以 | 调用", "适合格式化/转换类逻辑", "保持模板简洁", "可全局复用"]),

"2.4 Django Templates": (
"Django 模板语言（DTL）与 Jinja2 风格相似但有自己的语法与内置标签体系。",
"通过 {% block %}/{% extends %} 实现继承，{% for %}/{% if %} 控制流，模板标签与过滤器构成展示逻辑；Django 默认自动转义。",
["block/extends 继承体系", "内置标签与过滤器丰富", "默认自动转义", "与 Jinja2 语法相近但不同", "可用 Django 模板后端统一渲染"]),

"3.1 架构图": (
"前后端分离架构图帮助理解请求链路与职责划分，是方案沟通的基石。",
"典型链路：浏览器/App → 前端（SPA）→ 后端 API（Django/Flask/FastAPI）→ 数据库/缓存；前后端通过 JSON 接口契约协作。",
["前端负责视图与交互", "后端提供 JSON API", "通过 HTTP 接口契约协作", "常见 SPA + REST/GraphQL", "可配合网关与 CDN"]),

"3.2 跨域（CORS）": (
"CORS 解决浏览器同源策略限制，使前端能安全访问不同源的 API。",
"浏览器预检（OPTIONS）请求携带 Origin，服务端通过 Access-Control-Allow-Origin 等响应头授权；简单请求与带凭证请求规则不同。",
["同源策略限制跨域请求", "服务端返回 Allow-Origin 头授权", "预检 OPTIONS 携带 Origin", "带 Cookie 需 Allow-Credentials", "Flask-CORS/Django-cors-headers 封装"]),

"3.3 认证": (
"前后端分离下的认证机制保障接口安全，常见为 JWT/Session/OAuth2。",
"JWT 将用户信息编码进无状态 Token，前端存于内存/localStorage 并在 Header 携带；Session 依赖服务端状态；OAuth2 用于第三方授权。",
["JWT 无状态、前端携带", "Session 依赖服务端存储", "Token 放 Authorization 头", "注意 XSS 窃取 Token 风险", "刷新 Token 提升安全性"]),

"3.4 开发环境代理": (
"开发代理让前端 dev server 把 API 请求转发到后端，规避跨域并统一路径。",
"Vite/Webpack 的 proxy 配置把 /api 代理到后端地址，浏览器只同源访问前端端口；生产则由 Nginx 统一反代。",
["dev server 代理 /api 到后端", "开发期规避跨域", "生产用 Nginx 反代", "路径前缀统一便于部署", "避免硬编码后端地址"]),

"4.1 Django 静态资源": (
"Django 的静态资源管理区分开发期与收集期，是模板正确加载 CSS/JS 的前提。",
"开发用 STATIC_URL 直接服务；生产用 collectstatic 将各 app 静态文件汇总到 STATIC_ROOT，由 Nginx 直接托管提升性能。",
["STATIC_URL 配置访问前缀", "collectstatic 收集到 STATIC_ROOT", "生产由 Nginx 直接托管", "DEBUG=False 需配置静态服务", "避免动态服务静态文件"]),

"4.2 前端构建集成": (
"前端构建集成把打包产物接入后端模板或静态目录，实现一体化部署。",
"前端用 Vite/Webpack 构建产物（JS/CSS），后端模板引用或复制到静态目录；可通过 manifest 做缓存破坏（cache busting）。",
["构建产物交由后端托管", "模板引用带 hash 的资源", "支持缓存破坏", "CI 中先构建再收集", "前后端版本需对齐"]),

"4.3 Nginx 静态资源配置": (
"用 Nginx 直接托管静态资源可大幅降低后端压力并提升加载速度。",
"location 规则将 /static 映射到文件目录，expires 设置浏览器缓存，gzip 压缩传输；动态请求再反代到应用服务器。",
["location 映射静态目录", "expires 设置缓存", "gzip 开启压缩", "静态由 Nginx 直接返回", "动态请求反代到后端"]),
},

"Python安全防护知识点系统梳理_优化版.md": {
"2.1 密码安全": (
"密码安全是账户体系的底线，错误的存储方式会导致大规模泄露。",
"绝不明文存储；应使用慢哈希算法（bcrypt/argon2/scrypt）加盐哈希，使暴力破解成本极高；校验时比较哈希而非原文。",
["禁止明文/可逆加密存储", "使用 bcrypt/argon2/scrypt 加盐哈希", "校验比对哈希而非原文", "加盐防止彩虹表攻击", "考虑加入登录限流防爆破"]),

"2.2 JWT 认证": (
"JWT 让无状态服务也能安全识别用户身份，广泛用于前后端分离。",
"服务端签名生成 Token（header.payload.signature），客户端携带于 Authorization 头；服务端验签即可信任，无需查库会话。",
["服务端签名、客户端携带", "签名保证不可篡改", "无状态、易水平扩展", "注意过期与刷新机制", "密钥泄露即全面失效"]),

"2.3 OAuth2": (
"OAuth2 是授权框架，允许第三方在用户授权下访问资源而不暴露密码。",
"通过授权码（authorization code）等模式，由授权服务器颁发 access token；常见微信/Google/GitHub 登录即基于它。",
["授权而非认证密码共享", "授权码模式最安全", "颁发 access/refresh token", "适合第三方登录", "需妥善保护 client_secret"]),

"3.1 SQL 注入": (
"SQL 注入可在拼接 SQL 时执行攻击者构造的恶意语句，是最常见高危漏洞之一。",
"成因是把用户输入直接拼进 SQL 字符串；参数化查询/ORM 会把输入当作数据而非代码，从根本上杜绝注入。",
["成因：拼接用户输入到 SQL", "用参数化查询/ORM 防注入", "避免字符串格式化拼 SQL", "最小权限数据库账户", "输入校验作为纵深防御"]),

"3.2 命令注入": (
"命令注入在调用系统命令时执行了攻击者插入的额外指令，危害极大。",
"成因是用 shell 拼接用户输入执行 subprocess；应使用列表形式传参、避免 shell=True，并对输入严格白名单。",
["避免 shell=True 拼接", "用列表参数调用 subprocess", "输入白名单校验", "最小权限运行进程", "不回显原始命令错误"]),

"3.3 代码注入": (
"代码注入通过 eval/exec 或模板注入执行任意代码，是极高危漏洞。",
"避免对用户输入使用 eval/exec/__import__；模板渲染禁用危险的 sandbox 外功能；对动态执行保持零信任。",
["禁用 eval/exec 处理输入", "模板渲染启用沙箱", "动态导入需白名单", "最小暴露内部对象", "代码审计重点排查动态执行"]),

"4.1 类型": (
"了解 XSS 的类型有助于针对性防护：存储型、反射型、DOM 型。",
"存储型入库后展示给用户；反射型通过 URL 参数即时回显；DOM 型在浏览器端脚本中触发；三者都源于不可信内容被当作代码执行。",
["存储型：入库后展示", "反射型：URL 参数回显", "DOM 型：前端脚本触发", "均因未转义不可信内容", "危害为窃取 Cookie/劫持会话"]),

"4.2 防护": (
"XSS 防护核心是让不可信内容永远不被当作可执行代码。",
"输出到 HTML 时自动转义（框架默认开启）、对富文本做白名单净化（bleach）、设置 CSP 限制脚本来源、敏感操作避免依赖 Cookie 自动携带。",
["输出自动转义", "富文本用白名单净化", "设置 Content-Security-Policy", "HttpOnly Cookie 防 JS 读取", "避免 innerHTML 拼接不可信内容"]),

"5.1 原理": (
"理解 CSRF 原理才能正确配置防护；它利用用户已登录状态伪造请求。",
"攻击者诱导已登录用户在第三方页面提交请求，浏览器自动附带 Cookie；若服务端仅依赖 Cookie 鉴权即会误执行。",
["利用自动携带的 Cookie", "伪造跨站请求", "针对状态变更操作", "需用户已登录", "GET 不应做变更操作"]),

"5.2 防护": (
"CSRF 防护通过验证请求来源或携带的随机凭证来阻断伪造。",
"常用 CSRF Token（表单/Header 中随请求提交并由服务端校验）、SameSite Cookie 限制跨站携带、校验 Origin/Referer。",
["CSRF Token 校验", "SameSite=Strict/Lax Cookie", "校验 Origin/Referer", "敏感操作要求二次确认", "配合框架中间件统一防护"]),

"7.1 安全响应头": (
"安全响应头是低成本高收益的纵深防御手段，能缓解多种前端攻击。",
"如 Content-Security-Policy 限制资源加载、X-Content-Type-Options 防 MIME 嗅探、X-Frame-Options 防点击劫持、Strict-Transport-Security 强制 HTTPS。",
["CSP 限制脚本/资源来源", "X-Frame-Options 防点击劫持", "HSTS 强制 HTTPS", "X-Content-Type-Options 防嗅探", "可经 Nginx/框架统一设置"]),

"7.2 CORS 配置": (
"合理的 CORS 配置在开放跨域能力的同时避免过度授权。",
"精确设置 Access-Control-Allow-Origin（避免盲目 * 配合凭证）、限制方法与头、对带凭证请求显式声明来源；错误配置会放大 XSS 影响。",
["避免 Allow-Origin:* + 凭证", "显式声明允许的来源", "限制方法与请求头", "凭证请求需精确匹配", "与安全头协同配置"]),
},

"Python性能优化知识点系统梳理_优化版.md": {
"2.1 cProfile（函数级）": (
"cProfile 是标准库性能剖析工具，快速定位最耗时的函数。",
"基于确定性采样统计每个函数的调用次数与累计/自身耗时；配合 pstats 排序查看热点，无需修改业务代码。",
["标准库、零侵入", "统计函数调用次数与耗时", "pstats 按耗时排序找热点", "适合整体瓶颈定位", "开销较低可生产采样"]),

"2.2 line_profiler（行级）": (
"line_profiler 将耗时细化到每一行代码，定位函数内部热点。",
"用 @profile 装饰目标函数，kernprof 运行后输出逐行命中次数与耗时；适合在 cProfile 锁定函数后进一步下钻。",
["逐行统计耗时", "@profile 标记目标函数", "配合 kernprof 运行", "开销大于 cProfile", "用于函数内精细优化"]),

"2.3 memory_profiler（内存）": (
"memory_profiler 观测内存随时间变化，定位内存泄漏与暴涨。",
"用 @profile 装饰函数后逐行报告内存增量；也可周期性监控进程内存，配合 objgraph 找泄漏对象引用链。",
["逐行报告内存增量", "定位内存泄漏", "配合 objgraph 找引用", "运行较慢", "适合内存问题排查"]),

"2.4 其他工具": (
"除 cProfile 外，py-spy、snakeviz、scalene 等工具覆盖不同剖析场景。",
"py-spy 可采样正在运行的进程（无需改代码）；scalene 同时分析 CPU/内存/GPU；snakeviz 可视化 cProfile 结果。",
["py-spy 采样运行中进程", "scalene 多维度剖析", "snakeviz 可视化火焰图", "按场景选工具", "先度量再优化"]),

"3.1 循环优化": (
"循环往往是 Python 性能热点，优化循环能带来显著收益。",
"用内置函数（map/sum）、列表推导替代显式 for、将不变计算移出循环、用局部变量减少属性查找；必要时用 NumPy 向量化。",
["推导式替代显式循环", "不变计算移出循环", "用局部变量减少查找", "借助内置函数", "大数据用 NumPy 向量化"]),

"3.2 字符串拼接": (
"字符串不可变，错误的拼接方式会产生大量中间对象。",
"多次拼接应用 join() 或 io.StringIO，避免在循环中用 += 反复生成新字符串；f-string 适合少量格式化。",
["用 join 拼接多段", "循环避免 += 拼接", "StringIO 处理大量拼接", "f-string 做格式化", "减少中间字符串对象"]),

"3.3 函数调用优化": (
"Python 函数调用有一定开销，高频小函数可酌情优化。",
"用局部变量缓存全局/属性访问、用内置函数替代手写、用 functools.lru_cache 缓存纯函数结果、考虑内联热点。",
["局部变量缓存属性访问", "lru_cache 缓存纯函数", "用内置函数替代手写", "减少不必要的小函数调用", "热点处权衡可读性"]),

"3.4 数据结构选择": (
"选对数据结构能让操作从 O(n) 降到 O(1)，是性价比最高的优化。",
"查找多用 set/dict，计数用 Counter，有序用 heapq，去重保序用 dict/OrderedDict；避免列表线性查找。",
["查找用 set/dict", "计数用 Counter", "TopK 用 heapq", "避免 list 线性查找", "按操作复杂度选型"]),

"3.5 生成器节省内存": (
"生成器惰性产出元素，能把内存占用从 O(n) 降到 O(1)。",
"用 yield 或生成器表达式逐个产出，不必一次性构造完整列表；适合大文件、大集合的流式处理。",
["yield 惰性产出", "避免一次性建大列表", "适合流式处理", "配合 itertools 组合", "注意只能遍历一次"]),

"4.1 多进程（CPU 密集）": (
"CPU 密集型任务应优先用多进程绕过 GIL 以利用多核。",
"multiprocessing 每个进程独立解释器与 GIL，真正并行；适合计算密集；代价是进程间通信与内存开销较大。",
["绕过 GIL 利用多核", "适合计算密集", "进程间通信开销大", "可用 ProcessPoolExecutor", "注意序列化传参"]),

"4.2 多线程（IO 密集）": (
"IO 密集型任务用多线程可在等待 IO 时让出 GIL，提升吞吐。",
"threading 在 IO 等待时释放 GIL，适合网络/文件 IO；注意共享状态需加锁（Lock），且不适用于 CPU 密集。",
["IO 等待时让出 GIL", "适合网络/文件 IO", "共享状态需加锁", "不适用于 CPU 密集", "用 ThreadPoolExecutor 简化"]),

"4.3 asyncio（异步 IO）": (
"asyncio 用单线程协程处理海量并发 IO，资源占用低、扩展性高。",
"事件循环调度协程，await 挂起等待 IO 时不阻塞其他任务；适合高并发网络请求，但要求 IO 库本身支持异步。",
["单线程高并发 IO", "await 挂起不阻塞", "事件循环调度", "需异步库配合", "调试比线程复杂"]),

"6.1 `__slots__`": (
"__slots__ 通过固定实例属性、取消 __dict__ 来省内存并加速访问。",
"声明 __slots__ 后实例不再有动态 __dict__，属性存于紧凑结构；对创建海量对象（如 ORM 行）效果显著。",
["取消 __dict__ 省内存", "属性访问更快", "禁止动态增删属性", "适合海量小对象", "继承链都需声明"]),

"6.2 生成器/迭代器": (
"生成器与迭代器是内存友好的惰性序列抽象。",
"迭代器实现 __iter__/__next__，生成器用 yield 自动实现；二者都按需产出，避免一次性加载全部数据。",
["惰性按需产出", "避免全量驻留内存", "生成器自动实现迭代协议", "可无限序列", "配合 for 消费"]),

"6.3 数组模块": (
"array 模块用类型化紧凑数组替代 list，降低数值存储开销。",
"array.array 存储同类型原始值（如 int/float），比 list 的对象指针更省内存且缓存友好；大量数值时优于 list。",
["同类型紧凑存储", "比 list 省内存", "适合大量数值", "操作类似 list", "极致可用 NumPy"]),

"6.4 及时释放": (
"及时释放不再使用的大对象能降低峰值内存与 GC 压力。",
"显式 del 引用、将大变量置 None、缩小变量作用域，让引用计数归零即可被回收；循环引用交标记-清除处理。",
["del 删除引用", "大对象及时置 None", "缩小作用域", "引用归零即回收", "避免意外长期持有"]),

"7.1 Cython": (
"Cython 将 Python 代码编译为 C 扩展，显著提升计算密集代码速度。",
"用静态类型标注（cdef）后编译为 C 扩展模块，绕过解释器开销；适合热点算法，但需编写构建配置。",
["编译为 C 扩展提速", "静态类型标注增益", "适合计算热点", "需构建步骤", "可渐进式改造"]),

"7.2 PyPy": (
"PyPy 是带 JIT 的 Python 实现，对长运行纯 Python 程序加速明显。",
"JIT 把热点字节码编译为机器码；无需改代码即可提速，但兼容性（C 扩展、GIL）与冷启动需评估。",
["JIT 即时编译加速", "多数无需改代码", "适合长运行程序", "C 扩展兼容性需验证", "仍有 GIL 限制"]),

"7.3 numba": (
"numba 用 JIT 把带类型的数值函数编译为机器码，几乎零改造提速。",
"@njit 装饰数值函数，LLVM 编译为原生代码；对 NumPy 风格循环加速极佳，但不支持全部 Python 特性。",
["@njit JIT 编译", "对数值/NumPy 友好", "几乎零改造", "不支持全部语法", "适合科学计算热点"]),
},

"Python接口设计与文档知识点系统梳理_优化版.md": {
"2.1 核心原则": (
"RESTful 核心原则让 API 一致、可预测、易进化，是接口设计的基石。",
"以资源为中心、用 HTTP 方法表达动作、用状态码表达结果、无状态；统一约定降低前端接入成本。",
["资源为中心、URL 表名词", "HTTP 方法表动作", "状态码表结果", "服务端无状态", "统一约定降低耦合"]),

"2.2 URL 设计规范": (
"规范的 URL 提升可读性与可维护性，避免歧义与冗余。",
"用名词复数（/articles）、层级表达关系（/articles/{id}/comments）、避免动词与文件后缀；用连字符而非下划线。",
["名词复数表资源", "层级表达从属关系", "避免 URL 中出现动词", "不用文件后缀", "连字符优于下划线"]),

"2.3 分页": (
"分页避免一次性返回海量数据，保护后端与网络。",
"常用 offset/limit 或 cursor（游标）分页；cursor 基于有序键更适合大数据与实时变化场景，offset 实现简单但深翻页慢。",
["offset/limit 简单", "cursor 适合深翻页", "返回总数/下一页标识", "限制单页大小上限", "避免无上限查询"]),

"2.4 过滤、排序、搜索": (
"统一的查询参数约定让列表接口灵活且可组合。",
"用 filter[field]=、sort=、q= 等约定表达过滤/排序/搜索；后端做白名单校验，防止任意字段暴露与注入。",
["统一参数约定", "过滤/排序/搜索分离", "后端字段白名单", "防止 SQL/NoSQL 注入", "缺省合理排序"]),

"3.1 成功响应": (
"统一的成功响应结构让前端解析一致、易于封装。",
"常见 {code, data, message} 或直接使用 HTTP 状态+data；约定好字段命名（如 data/list/meta）可让客户端通用处理。",
["结构统一便于封装", "HTTP 状态表达成功", "data 承载业务数据", "meta 携带分页信息", "命名风格前后端一致"]),

"3.2 错误响应": (
"规范的错误响应帮助前端精准处理异常与提示用户。",
"返回合适状态码（4xx/5xx）与结构化错误体（code+message+detail）；避免把堆栈暴露给用户，敏感信息写入日志。",
["用状态码区分错误类型", "结构化错误体", "不暴露内部堆栈", "detail 给可操作信息", "敏感错误仅记日志"]),

"3.3 错误码设计": (
"业务错误码在 HTTP 状态之上进一步细分，便于定位与国际化。",
"用稳定、可枚举的数字/字符串码（如 AUTH_001），与 HTTP 状态解耦；前端按码做精确分支与文案映射。",
["稳定可枚举的码", "与 HTTP 状态互补", "便于日志检索", "支持国际化映射", "避免硬编码散落"]),

"4.1 版本策略": (
"API 版本管理让接口演进不破坏已有客户端。",
"常用 URL（/v1/...）、Header（Accept-Version）或 Query（?version=）承载版本；URL 版本最直观、最易调试。",
["URL 版本最直观", "Header 版本更干净", "Query 版本易兼容", "版本从 v1 起", "废弃需提前通告"]),

"4.2 向后兼容原则": (
"向后兼容原则保障老客户端在接口迭代时仍可工作。",
"新增字段不删不改旧字段、不收缩枚举、保持语义；破坏性变更必须升版本；用弃用周期平滑过渡。",
["新增不破坏旧客户端", "不删不改既有字段", "破坏性变更升版本", "设弃用过渡期", "文档标注弃用"]),

"5.1 FastAPI 自动文档": (
"FastAPI 基于类型提示自动生成交互式 API 文档，省去手工维护。",
"通过 Pydantic 模型与路径操作自动产出 OpenAPI，提供 /docs（Swagger UI）与 /redoc；类型即文档，改动代码即更新。",
["类型提示驱动文档", "内置 Swagger/ReDoc", "OpenAPI 可导出", "改动即同步", "便于前后端对接"]),

"5.2 Django REST Framework + drf-spectacular": (
"drf-spectacular 为 DRF 生成标准 OpenAPI 文档，弥补原生缺文档短板。",
"基于 SpectacularExtension 与序列化器/视图注解自动推导 schema；可导出 YAML/JSON 并接入 Swagger UI，支持自定义扩展。",
["自动推导 OpenAPI", "基于序列化器注解", "可导出标准 schema", "接入 Swagger UI", "支持自定义扩展"]),

"5.3 Flask-RESTX / Flask-OpenAPI": (
"Flask 生态通过扩展生成 API 文档，弥补其无内置文档的不足。",
"Flask-RESTX 用命名空间与模型声明生成 Swagger；Flask-OpenAPI（如 flasgger/apispec）基于注解产出 OpenAPI，与 Marshmallow 集成好。",
["Flask-RESTX 自带 Swagger", "flasgger/apispec 产 OpenAPI", "配合 Marshmallow 模型", "需显式声明模型", "弥补无内置文档"]),

"5.4 文档最佳实践": (
"良好文档实践让 API 真正可用、可维护、可被机器消费。",
"保持代码与文档同源（自动生成优先）、给出可运行示例、标注鉴权与错误、提供变更日志；文档应随代码评审更新。",
["代码文档同源", "提供可运行示例", "标注鉴权与错误", "维护变更日志", "随代码评审更新"]),

"6.1 核心概念": (
"GraphQL 用单一端点与声明式查询解决 REST 的过度/不足获取问题。",
"客户端在 query 中精确声明所需字段，服务端按 schema 解析返回；Schema 定义类型与查询，Resolver 提供数据。",
["单一端点声明式查询", "Schema 定义类型", "Resolver 取数", "避免过度/不足获取", "强类型自描述"]),

"6.2 Python 实现": (
"Python 中可用 graphene/Strawberry 等库落地 GraphQL 服务。",
"用类型类定义 schema、用 resolver 方法取数；Strawberry 基于类型注解更现代，graphene 生态成熟；常与 Django/SQLAlchemy 集成。",
["graphene 生态成熟", "Strawberry 基于类型注解", "resolver 提供数据", "可与 ORM 集成", "注意 N+1（dataloader）"]),

"6.3 REST vs GraphQL": (
"理解二者取舍有助于按场景选型，而非盲目追新。",
"REST 简单、缓存友好、生态成熟；GraphQL 灵活、减少请求次数、强类型，但缓存与复杂度更高；按客户端多样性选择。",
["REST 简单缓存友好", "GraphQL 灵活少请求", "GraphQL 缓存更复杂", "强类型自描述", "按客户端需求选型"]),

"7.1 认证方式": (
"接口安全从认证开始，不同认证方式适配不同场景。",
"API Key 适合服务间简单调用，JWT 适合无状态前后端分离，OAuth2 适合第三方授权，Session 适合传统同域 Web。",
["API Key 服务间简单调用", "JWT 无状态前后端分离", "OAuth2 第三方授权", "Session 传统同域", "按场景选型"]),

"7.2 安全措施": (
"接口层安全措施构建纵深防御，堵住常见攻击面。",
"输入校验、输出编码、速率限制、HTTPS、最小权限、审计日志共同构成防线；任何单点都不足以保证安全。",
["输入校验与白名单", "HTTPS 全程加密", "最小权限原则", "审计与监控", "纵深防御"]),

"8.1 工具": (
"接口测试工具覆盖手动调试到自动化，保障 API 质量。",
"Postman/HTTPie 做手动与集合测试，pytest + requests/httpx 做自动化，Newman 把集合纳入 CI；契约测试保证前后端一致。",
["Postman 手动/集合测试", "pytest+requests 自动化", "Newman 接入 CI", "契约测试保一致", "按层级组合"]),

"8.2 Pytest 示例": (
"用 pytest 编写接口测试可纳入回归，验证状态码、结构与字段。",
"组织为 given/when/then，断言 status_code、JSON 关键字段与错误码；配合 fixture 管理 base_url 与鉴权 token。",
["断言状态码与结构", "fixture 管理 base_url/token", "覆盖成功与失败用例", "纳入 CI 回归", "数据与断言分离"]),
},

"Python数据库与缓存知识点系统梳理_优化版.md": {
"2.1 核心概念": (
"理解 SQLAlchemy 的核心抽象是高效使用它的前提。",
"Engine 管理连接池，Session 是工作单元（ORM 对话），MetaData 记录表结构，ORM 把类映射为表；1.x 命令式与 2.0 声明式风格并存。",
["Engine 管理连接池", "Session 是工作单元", "MetaData 描述结构", "ORM 映射类与表", "2.0 风格统一 sync/async"]),

"2.2 模型定义（2.0 风格）": (
"2.0 风格用 Mapped/declared_attr 等类型化声明，兼顾清晰与可检查。",
"用 DeclarativeBase + Mapped[Type] 声明列与关系，类型注解驱动；relationship 表达一对多/多对多，可配合类型检查。",
["DeclarativeBase 基类", "Mapped[Type] 类型化列", "relationship 表达关系", "注解驱动更清晰", "兼容旧式 Column 写法"]),

"2.3 基本操作": (
"掌握增删改查与会话提交是日常 ORM 开发的基础。",
"add() 入会话、commit() 落库、query()/select() 查询、flush() 推到数据库；理解会话的脏检查与事务边界。",
["add 入会话", "commit 提交事务", "select/query 查询", "flush 推库未提交", "注意会话生命周期"]),

"2.4 关系查询与 N+1 优化": (
"关系查询的 N+1 是常见性能坑，需用预加载消除。",
"lazy 加载关联会触发大量额外查询；用 joinedload（JOIN 一次查出）或 selectinload（IN 查询）预加载，按关系类型选择。",
["N+1 来自懒加载", "joinedload 用 JOIN", "selectinload 用 IN", "按关系类型选型", "监控查询数量"]),

"3.1 SQLAlchemy 2.0 Async": (
"异步 SQLAlchemy 让 ORM 在 asyncio 中执行而不阻塞事件循环。",
"使用 AsyncSession 与 async engine，await session.execute(select(...))；底层基于 asyncio 驱动（如 asyncpg），适合 FastAPI 异步栈。",
["AsyncSession 异步会话", "await execute(select)", "底层 asyncpg 等驱动", "配合 FastAPI 异步", "事务用 async with"]),

"3.2 其他异步库": (
"除 SQLAlchemy Async 外，aiomysql/asyncpg/Tortoise 等覆盖不同数据库与风格。",
"asyncpg 是 PostgreSQL 高性能驱动，Tortoise ORM 提供 Django 风格异步模型，databases 库包装多种异步驱动。",
["asyncpg 高性能 PG", "Tortoise 异步 ORM", "databases 多驱动封装", "适配 asyncio", "按数据库选型"]),

"4.1 基本使用": (
"Redis 是最常用的缓存与数据结构存储，掌握基本命令是缓存层基础。",
"通过 SET/GET 存取值、EXPIRE 设过期、用 String/Hash/List 等结构；Python 用 redis-py 或异步 aioredis 客户端。",
["SET/GET 基本读写", "EXPIRE 设过期", "多种数据结构", "redis-py/aioredis 客户端", "连接池复用连接"]),

"4.2 缓存装饰器模式": (
"缓存装饰器把“函数结果→缓存”逻辑收敛复用，减少样板。",
"用 @cache 装饰器以参数生成 key、查缓存命中则返回、未命中执行原函并存缓存；注意 key 命名空间与失效策略。",
["装饰器封装缓存逻辑", "参数生成缓存 key", "命中即返回", "注意 key 命名空间", "设置合理过期"]),

"4.5.1 ACID 与隔离级别": (
"事务 ACID 与隔离级别决定并发下的数据正确性与性能取舍。",
"原子性/一致性/隔离性/持久性；隔离级别从读未提交到串行化，越高越安全但并发越低，常用可重复读/读已提交。",
["ACID 四特性", "隔离级别递增安全", "读已提交/可重复读常用", "串行化性能最低", "按业务容忍度选型"]),

"4.5.2 SQLAlchemy 事务管理": (
"合理管理事务边界能保证数据一致并避免长事务锁表。",
"用 session.begin()/commit()/rollback() 显式控制，或用 async with session.begin() 自动提交/回滚；异常时回滚保证原子。",
["begin/commit/rollback", "异常自动回滚", "控制事务边界", "避免长事务", "嵌套用 savepoint"]),

"索引类型": (
"不同索引类型适配不同查询模式，选错会既慢又占空间。",
"B+Tree 适合范围/排序，Hash 适合等值，联合索引遵循最左前缀，唯一索引保证唯一；覆盖索引可避免回表。",
["B+Tree 通用", "Hash 等值快", "联合索引最左前缀", "唯一索引保唯一", "覆盖索引免回表"]),

"EXPLAIN 执行计划": (
"EXPLAIN 让你看见查询如何执行，是定位慢查询的利器。",
"查看 type（访问类型）、rows（扫描行数）、key（命中索引）、Extra（如 Using filesort）；全表扫描与 filesort 是优化重点。",
["看 type 访问类型", "看 rows 扫描行数", "看 key 命中索引", "关注 Using filesort", "目标减少扫描量"]),

"索引使用原则": (
"遵循索引使用原则才能让索引真正被命中。",
"遵循最左前缀、不在索引列上做函数/运算、避免隐式类型转换、区分度高才建索引、避免过多索引拖慢写。",
["最左前缀匹配", "索引列避免函数运算", "避免隐式类型转换", "高区分度才建", "索引过多拖慢写"]),

"5.1 查询优化": (
"查询优化从减少扫描、避免 N+1、精简返回字段入手。",
"用索引、只查所需列（defer/only）、分页限制、预加载关联、避免 SELECT * 与 ORM 默认全字段加载。",
["用索引加速", "只取所需列", "预加载防 N+1", "分页限制结果", "避免 SELECT *"]),

"5.2 连接池": (
"连接池复用数据库连接，避免频繁建立连接的开销。",
"Engine 维护池（size/overflow 配置），请求借连接、用完归还；池过小会排队，过大占用数据库资源。",
["复用连接省开销", "配置 pool_size/overflow", "用毕归还连接", "注意连接泄漏", "异步用异步池"]),

"5.3 读写分离": (
"读写分离把读流量分摊到从库，提升整体吞吐与可用性。",
"写走主库、读走从库（可能有复制延迟）；通过绑定引擎/路由实现，对一致性要求高的读需走主库。",
["写主读从", "提升读吞吐", "注意复制延迟", "强一致读走主", "借助 ORM 路由"]),
},

"Python测试工程知识点系统梳理_优化版.md": {
"2.1 基本用法": (
"pytest 以简洁断言与自动发现降低测试编写门槛，是 Python 测试事实标准。",
"函数名以 test_ 开头被自动收集，用原生 assert 断言；命令行运行并输出失败细节，无需 unittest 的样板。",
["test_ 函数自动发现", "原生 assert 断言", "失败信息清晰", "运行简单", "生态插件丰富"]),

"2.2 Fixture（夹具）": (
"Fixture 提供可复用的测试前置/后置与依赖注入，消除重复 setup。",
"用 @pytest.fixture 定义，测试函数以参数引用；支持 scope（function/module/session）控制生命周期，yield 做清理。",
["@pytest.fixture 定义", "参数注入复用", "scope 控制生命周期", "yield 做清理", "依赖可组合"]),

"2.3 参数化测试": (
"参数化让同一测试逻辑覆盖多组数据，减少重复代码。",
"用 @pytest.mark.parametrize 传入多组参数，框架自动展开为多个用例；便于边界值与等价类覆盖。",
["parametrize 多组数据", "自动展开用例", "覆盖边界值", "减少重复代码", "失败定位到具体参数"]),

"2.4 异常测试": (
"验证代码在异常路径上的行为，保证错误处理正确。",
"用 pytest.raises(ExpectedError) 断言抛出指定异常，可进一步校验异常消息/属性，确保失败被恰当暴露。",
["pytest.raises 断言异常", "可校验异常消息", "覆盖错误分支", "验证错误契约", "不要吞掉异常"]),

"4.1 unittest.mock": (
"unittest.mock 通过替身对象隔离外部依赖，使单元测试快速稳定。",
"用 Mock/MagicMock 替代依赖，patch 临时替换目标；可断言调用次数与参数，验证交互行为。",
["Mock 替代依赖", "patch 临时替换", "断言调用行为", "隔离外部服务", "注意及时恢复"]),

"4.2 pytest-mock": (
"pytest-mock 在 pytest 中提供更易用的 mocker 夹具做打桩。",
"mocker.patch/mocker.Mock 与 fixture 生命周期绑定，自动清理；比手动 unittest.patch 更简洁安全。",
["mocker 夹具自动清理", "patch 用法简洁", "与 pytest 集成好", "避免泄漏打桩", "适合 pytest 项目"]),

"6.1 FastAPI 测试": (
"FastAPI 提供 TestClient/AsyncClient 在测试中不启真实服务器即可验接口。",
"用 TestClient 发起请求，结合依赖覆盖（dependency_overrides）替换数据库/鉴权；断言状态码与响应体。",
["TestClient 内存测试", "覆盖依赖解耦", "断言状态与结构", "不需起服务", "异步用 httpx AsyncClient"]),

"6.2 Django 测试": (
"Django 测试基于 TestCase 与测试数据库，保障模型/视图/接口正确。",
"用例运行在事务回滚的测试库，TestCase 提供 client 发请求、assert 系列方法；fixtures 或 factory 造数据。",
["专用测试数据库", "TestCase 提供 client", "事务回滚隔离", "断言系列丰富", "可用 factory_boy 造数"]),
},

"Python语言基础与进阶知识点系统梳理_优化版.md": {
"2.1 基本类型": (
"掌握 Python 基本类型及其不可变/可变特性，是写出正确代码的基础。",
"int/float/bool/str/bytes 为不可变，None 是单例；理解不可变意味着赋值/传参是引用共享，修改会生成新对象。",
["int/float/bool/str 不可变", "None 是单例", "传参共享引用", "不可变改即新建", "数值有 int 无限精度"]),

"2.2 容器类型": (
"list/tuple/set/dict 各有语义与复杂度，选型影响正确性与性能。",
"list 有序可变、tuple 有序不可变、set 去重无序、dict 键值映射（3.7+ 保序）；理解可变对象作默认参数的陷阱。",
["list 可变有序", "tuple 不可变有序", "set 去重", "dict 保序映射", "警惕可变默认参数"]),

"2.3 字符串操作": (
"字符串处理是日常高频任务，熟悉方法能少写很多循环。",
"str 提供 split/join/format/f-strings、切片与常用方法；f-string 可读性高且性能好，注意大字符串拼接效率。",
["f-string 格式化", "split/join 切分合并", "切片灵活", "方法丰富", "避免循环 += 拼接"]),

"3.1 条件与循环": (
"条件与循环是控制流核心，写好它们影响可读与性能。",
"if/elif/else、for/while、推导式与 else 子句（循环正常结束才执行）；善用 enumerate/zip 减少索引操作。",
["if/elif/else 分支", "for/while 循环", "推导式简洁", "for-else 用法", "enumerate/zip 简化"]),

"3.2 函数": (
"函数是组织逻辑与复用的一等公民，理解其机制很关键。",
"支持默认参数、*args/**kwargs、关键字-only、闭包与高阶函数；注意默认参数用不可变对象、可变默认参数陷阱。",
["默认参数/可变参数", "关键字-only 参数", "闭包与高阶函数", "变量作用域 LEGB", "警惕可变默认参数"]),

"3.5.1 基础语法": (
"异常处理基础语法保证程序在错误时优雅失败而非崩溃。",
"try/except/else/finally 结构，except 捕获指定异常、else 无异常时执行、finally 必定执行（如释放资源）。",
["try/except 捕获", "else 无异常分支", "finally 必执行", "精准捕获异常", "不要用裸 except"]),

"3.5.2 自定义异常与异常链": (
"自定义异常与异常链提升错误的语义与可追溯性。",
"继承 Exception 定义业务异常；用 raise ... from 保留原始异常链（__cause__），便于排查根因。",
["继承 Exception 定制", "语义化错误类型", "raise ... from 保链", "便于定位根因", "不要吞掉底层异常"]),

"3.5.3 最佳实践": (
"异常最佳实践让错误处理既健壮又不影响正常路径可读性。",
"捕获具体异常而非基类、不在 try 放过多代码、资源用 with 管理、异常用于异常流而非控制流。",
["捕获具体异常", "try 块尽量小", "with 管理资源", "异常非控制流", "记录并合理上抛"]),

"4.1 类与继承": (
"面向对象用类封装状态与行为，继承实现复用与多态。",
"class 定义，__init__ 初始化，单继承为主、可多重继承（MRO 解析顺序）；super() 调用父类方法。",
["class 封装状态行为", "单继承为主", "super() 调父类", "MRO 决定查找顺序", "构造器需正确初始化"]),

"4.2 魔术方法（Dunder）": (
"魔术方法（双下划线）让自定义对象像内建类型一样工作。",
"__init__/__str__/__repr__ 表达初始化与展示，__len__/__getitem__ 支持容器协议，__eq__ 自定义相等；运算符也可重载。",
["__str__/__repr__ 展示", "__len__/__getitem__ 容器", "__eq__ 自定义相等", "运算符可重载", "用于协议对齐"]),

"4.3 封装与多态": (
"封装与多态是 OOP 两大支柱，提升模块边界与扩展能力。",
"封装用命名约定（_ 保护、__ 名称改写）隐藏实现；多态让不同对象以统一接口响应，配合 Duck Typing 更灵活。",
["_ 约定保护成员", "__ 名称改写", "多态统一接口", "Duck Typing 灵活", "面向接口编程"]),

"4.5.1 基础语法": (
"类型注解语法在不改变运行行为的前提下提升可读与可维护。",
"用变量: 类型、函数 -> 返回类型注解；注解仅提示，运行时不强制；需 typing 模块支持复杂类型。",
["变量/参数注解", "-> 返回注解", "运行时不强制", "提升可读性", "配 IDE 更友好"]),

"4.5.2 高级类型": (
"高级类型表达更复杂的结构，让接口契约更精确。",
"Optional/Union、List/Dict 泛型、Callable、TypeVar 泛型函数、Literal/Annotated；配合 typing 表达约束。",
["Optional/Union 联合", "泛型 List/Dict", "Callable 可调用", "TypeVar 泛型", "Literal/Annotated 细化"]),

"4.5.3 类型检查工具": (
"类型检查工具在运行前发现类型错误，提升大型项目可靠性。",
"mypy 是最流行静态检查器，pyright 由微软维护、速度快；配合 pre-commit 在提交时校验，需逐步加严。",
["mypy 主流静态检查", "pyright 快且强", "pre-commit 集成", "逐步加严配置", "提升重构安全"]),

"5.1 迭代器与生成器": (
"迭代器与生成器是 Python 惰性处理序列的基础抽象。",
"可迭代对象实现 __iter__，迭代器实现 __next__；生成器用 yield 自动实现迭代协议，惰性产出、节省内存。",
["可迭代 vs 迭代器", "yield 生成器", "惰性产出", "可无限序列", "配合 for 消费"]),

"5.2 上下文管理器": (
"上下文管理器用 with 自动管理资源获取与释放，避免泄漏。",
"实现 __enter__/__exit__ 或用 @contextmanager 装饰生成器；常用于文件、锁、数据库连接，异常也会正确清理。",
["with 自动清理", "__enter__/__exit__", "@contextmanager 简化", "异常也清理", "文件/锁/连接适用"]),

"5.3 装饰器进阶": (
"装饰器在不改动原函数的情况下增强其行为，是横切逻辑利器。",
"本质是接收函数返回函数；用 functools.wraps 保留元信息，支持带参装饰器与类装饰器；注意执行时机与顺序。",
["接收函数返回函数", "wraps 保留元信息", "支持带参装饰器", "类装饰器", "执行顺序由外到内"]),

"5.4.1 文件操作": (
"文件 IO 是持久化与数据交换的基础，用对方式避免资源泄漏。",
"open() 配合 with 自动关闭；文本/二进制模式区分，read/write/迭代逐行；注意编码（utf-8）与路径处理。",
["with open 自动关闭", "文本/二进制模式", "逐行迭代省内存", "指定编码 utf-8", "区分读写模式"]),

"5.4.2 数据序列化": (
"序列化让 Python 对象在存储/网络间转换，JSON 最通用。",
"json 模块做 dict/基本类型互转，pickle 可序列化任意对象但有安全风险；datetime 等需自定义编码器。",
["json 通用互转", "pickle 强但危险", "注意类型限制", "自定义编码器", "跨语言用 JSON"]),

"5.4.3 pathlib（面向对象路径）": (
"pathlib 用面向对象方式处理路径，跨平台且可读性优于 os.path。",
"Path 对象表达路径，/ 拼接、exists()/read_text()/glob() 等方法直观；自动处理不同系统分隔符。",
["Path 对象化路径", "/ 拼接直观", "read_text/write_text", "glob 模式匹配", "跨平台兼容"]),

"5.6.1 虚拟环境": (
"虚拟环境隔离项目依赖，避免全局包冲突与版本混乱。",
"venv 创建独立环境，activate 激活后 pip 安装仅作用于当前环境；每个项目应有独立环境。",
["venv 创建隔离环境", "activate 激活", "依赖互不干扰", "每项目一环境", "避免全局安装"]),

"5.6.2 包管理工具": (
"现代包管理工具提升依赖安装速度与可复现性。",
"pip 是标准；poetry/uv/pipenv 提供依赖解析、虚拟环境与锁定文件一体化；按团队习惯选型。",
["pip 标准", "poetry 一体化管理", "uv 极快", "pipenv 早期方案", "按团队选型"]),

"5.6.3 依赖锁定": (
"依赖锁定保证环境可复现，是部署与协作稳定的关键。",
"用 requirements.txt（pip freeze）或 poetry.lock/uv.lock 固定版本；区分依赖与开发依赖，CI 中用锁定文件安装。",
["lock 固定版本", "区分生产与开发依赖", "CI 用锁定安装", "避免意外升级", "提升可复现性"]),

"6.1 引用计数": (
"引用计数是 Python 主要的内存回收机制，简单高效。",
"每个对象记引用数，归零即回收；进出作用域、del、重新赋值都会增减计数；能即时释放大多数对象。",
["对象带引用计数", "归零即回收", "del/重赋值减计数", "即时释放", "无法处理循环引用"]),

"6.2 标记-清除（解决循环引用）": (
"标记-清除补偿引用计数的不足，处理循环引用对象。",
"定期从根对象出发标记可达对象，清除不可达的循环引用；配合分代回收减少扫描频率。",
["解决循环引用", "根可达性标记", "清除不可达对象", "与引用计数互补", "开销随对象增多"]),

"6.3 分代回收": (
"分代回收基于“对象越活越久越可能继续存活”的假设，降低 GC 成本。",
"对象分 0/1/2 三代，新对象频繁扫描、存活对象升代减少扫描；可用 gc 模块调参与手动触发。",
["对象分三代", "新对象高频扫描", "存活升代降频", "降低 GC 成本", "gc 模块可调"]),

"6.4 GIL（全局解释器锁）": (
"GIL 是 CPython 的单字节码执行锁，深刻影响并发模型选型。",
"同一进程内同一时刻仅一个线程执行 Python 字节码，故多线程不适合 CPU 密集；IO 密集仍可并发，CPU 密集用多进程或 C 扩展释放 GIL。",
["单进程单线程执字节码", "多线程不利 CPU 密集", "IO 密集仍可并发", "CPU 密集用多进程", "C 扩展可释放 GIL"]),

"7.1 多线程": (
"多线程适合 IO 密集型并发，但受 GIL 限制不适合计算密集。",
"threading 模块创建线程，IO 等待时让出 GIL；共享内存需 Lock 等同步原语避免竞态。",
["适合 IO 并发", "IO 让出 GIL", "共享内存需锁", "Thread 开销低", "不适合计算密集"]),

"7.2 多进程": (
"多进程绕过 GIL，真正并行执行 CPU 密集任务。",
"multiprocessing 每进程独立解释器，利用多核；代价是内存与 IPC 开销，适合计算密集与隔离场景。",
["绕过 GIL 多核并行", "适合计算密集", "内存开销大", "IPC 需序列化", "ProcessPoolExecutor 简化"]),

"7.3 异步编程（asyncio）": (
"asyncio 用协程在单线程内实现高并发 IO，资源占用低。",
"事件循环调度协程，await 挂起等待 IO；适合海量连接（如爬虫、网关），但要整套生态支持异步。",
["单线程协程并发", "事件循环调度", "await 非阻塞", "适合高并发 IO", "需异步生态"]),
},

"Python部署运维知识点系统梳理_优化版.md": {
"2.1 Gunicorn": (
"Gunicorn 是成熟的 WSGI 服务器，是部署 Django/Flask 的常用选择。",
"多 worker 预派生模型，主进程管理 worker；配合同步/异步 worker 类与 Nginx 反代；不支持原生 ASGI 需配 uvicorn worker。",
["预派生多 worker", "主进程管理", "配 Nginx 反代", "同步/异步 worker", "原生不支持 ASGI"]),

"2.2 uWSGI": (
"uWSGI 功能全面、配置强大的 WSGI 服务器，适合复杂部署。",
"支持多种协议、进程/线程混合模型与丰富调优；配置体系庞大，常与 Nginx 通过 socket 通信。",
["功能全面可深度调优", "进程/线程混合", "协议多样", "配置复杂", "常配 Nginx socket"]),

"2.3 Worker 类型选择": (
"合适的 worker 类型与数量匹配应用 IO/CPU 特征，决定吞吐与稳定。",
"同步 worker 适合 CPU 普通请求，gevent/异步 worker 适合高并发 IO；数量参考 (2×CPU)+1，并防内存膨胀。",
["同步/异步 worker", "gevent 高并发 IO", "数量≈2×CPU+1", "防内存膨胀", "按负载画像调"]),

"3.1 Uvicorn": (
"Uvicorn 是轻量高性能 ASGI 服务器，是运行 FastAPI 的首选。",
"基于 uvloop 与 httptools，支持 HTTP/1.1 与 WebSocket；常作为 Gunicorn 的 uvicorn worker 以获得进程管理。",
["ASGI 服务器", "uvloop 高性能", "支持 WebSocket", "常配 Gunicorn worker", "运行 FastAPI/Starlette"]),

"3.2 ASGI vs WSGI": (
"理解 ASGI 与 WSGI 差异是选型异步栈与评估并发能力的基础。",
"WSGI 同步、一连接一线程；ASGI 异步、单进程处理多并发连接，支持 WebSocket/HTTP2；ASGI 向后兼容 WSGI 应用。",
["WSGI 同步模型", "ASGI 异步并发", "ASGI 支持 WebSocket", "ASGI 兼容 WSGI", "按是否需要异步选型"]),

"4.1 Supervisor": (
"Supervisor 用纯 Python 管理进程，简化后台服务守护与自启。",
"通过配置文件定义要守护的进程，自动重启崩溃进程、集中日志；适合无 systemd 的环境（如容器/旧系统）。",
["纯 Python 进程管理", "崩溃自动重启", "集中日志", "配置简单", "适合容器/无 systemd"]),

"4.2 systemd": (
"systemd 是 Linux 主流初始化系统，适合生产服务化管理。",
"用 unit 文件定义服务、依赖与重启策略，支持开机自启、日志接入 journald；是服务器部署的标准方式。",
["系统级服务管理", "开机自启", "重启策略可配", "journald 日志", "生产标准方式"]),

"6.1 Dockerfile": (
"Dockerfile 定义应用镜像构建，是容器化部署的起点。",
"用基础镜像、依赖安装、代码拷贝、暴露端口与启动命令；多用多阶段构建减小体积、缓存依赖层加速构建。",
["基础镜像选型", "多阶段构建瘦身", "依赖层缓存", "非 root 运行", "明确 CMD/ENTRYPOINT"]),

"6.2 Docker Compose": (
"Docker Compose 用声明式配置编排多容器应用，简化本地与中小部署。",
"docker-compose.yml 定义服务、网络、卷与依赖；一条命令拉起 Web+DB+Cache 整套环境，便于开发与环境一致。",
["声明式多服务", "一键拉起整套", "共享网络与卷", "服务依赖编排", "适合开发/中小部署"]),

"7.1 GitHub Actions 示例": (
"CI/CD 自动化构建测试部署，保障质量与交付速度。",
"用 workflow YAML 定义触发条件与 job（lint/test/build/deploy）；矩阵测试多版本，部署步骤结合密钥与 SSH/Docker。",
["YAML 定义流水线", "触发条件灵活", "矩阵测多版本", "测试门禁", "部署结合密钥"]),

"8.1 日志": (
"规范日志是线上问题排查与可观测性的基础。",
"用 logging 模块分级（DEBUG/INFO/WARNING/ERROR）、结构化输出（JSON）、附带上下文；避免打印敏感信息、控制级别。",
["logging 分级", "结构化便于检索", "附上下文", "不记敏感信息", "按环境调级别"]),

"8.2 监控工具": (
"监控与指标帮助实时掌握系统健康并及时发现异常。",
"Prometheus 拉取指标、Grafana 展示、Sentry 收集错误；关注 QPS/延迟/错误率/资源使用率并设置告警。",
["Prometheus 指标", "Grafana 可视化", "Sentry 错误追踪", "关注黄金指标", "配置告警"]),

"9.1 蓝绿部署": (
"蓝绿部署用两套环境切换实现零停机发布与快速回滚。",
"同时运行蓝（旧）绿（新）两套，流量从蓝切到绿；出问题立刻切回蓝，风险低但需双倍资源。",
["双环境切换", "零停机发布", "秒级回滚", "需双倍资源", "切换前充分验证"]),

"9.2 滚动更新": (
"滚动更新逐步替换实例，在资源节约与可用间取得平衡。",
"按批次逐个用新版本替换旧实例，始终保持部分容量在线；需就绪探针防止流量打到未就绪实例。",
["分批替换实例", "始终保持可用", "资源占用低", "需健康检查", "回滚较慢"]),

"9.3 金丝雀发布（灰度发布）": (
"金丝雀发布先把新版本放少量流量，验证无碍再全量。",
"按权重将小部分请求路由到新版本，观察指标；异常则回退，降低故障爆炸半径，适合高风险变更。",
["小流量验证", "降低故障半径", "按指标决策", "需精细路由", "适合高风险变更"]),

"9.4 负载均衡算法": (
"负载均衡算法决定请求如何分发，影响性能与可用性。",
"轮询、加权轮询、最少连接、IP 哈希（会话保持）、一致性哈希等；按后端异构与服务特点选择。",
["轮询/加权轮询", "最少连接", "IP 哈希保会话", "一致性哈希减抖动", "按后端特点选型"]),
},

}
