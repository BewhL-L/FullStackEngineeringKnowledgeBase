---
title: 设计模式知识点系统梳理
tags: [后端, 设计模式, Java, 面向对象, 面试]
created: 2026-08-13
updated: 2026-08-13
---

# 设计模式知识点系统梳理（优化版）

> **优化版说明**：本文档在原有内容基础上，为每个三级标题知识点补充了「🔍 深度解析」（作用+原理+用法要点），所有原有内容完整保留，未做任何修改。


> **文档说明**：系统梳理 23 种经典设计模式，重点讲解常用模式的结构、应用场景和 Java 实现，涵盖创建型、结构型、行为型三大类。

---

## 1. 概述

设计模式是软件开发中针对常见问题的**可复用解决方案**，是前人经验的总结。GoF（Gang of Four）提出了 23 种经典设计模式，分为三大类。

**设计模式六大原则（SOLID + 迪米特）**：
| 原则 | 简称 | 说明 |
|------|------|------|
| 单一职责 | SRP | 一个类只做一件事 |
| 开闭原则 | OCP | 对扩展开放，对修改关闭 |
| 里氏替换 | LSP | 子类可替换父类 |
| 接口隔离 | ISP | 接口要小而专，不要大而全 |
| 依赖倒置 | DIP | 依赖抽象，不依赖具体 |
| 迪米特法则 | LoD | 最少知识，降低耦合 |

---


---
## 2. 创建型模式（5种）

### 2.1 单例模式（Singleton）

**保证一个类只有一个实例，并提供全局访问点。**

```java
// 双重检查锁（DCL）
public class Singleton {
    private static volatile Singleton instance;
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}

// 枚举单例（最安全，推荐）
public enum Singleton {
    INSTANCE;
    public void doSomething() {}
}
```

> 🔍 **知识点深度解析**
>
> **作用**：确保全局唯一实例，节省资源，常用于配置管理、连接池、日志对象。
>
> **原理**：构造方法私有，外部无法 new，通过静态方法获取唯一实例。DCL 双重检查保证线程安全且性能好，volatile 防止指令重排序（new 对象分三步：分配内存、初始化、引用赋值，重排序可能导致其他线程拿到未初始化对象）。枚举单例由 JVM 保证单例和线程安全，还能防止反射和序列化破坏。
>
> **用法要点**：① 优先用枚举实现单例；② DCL 必须加 volatile；③ 静态内部类也是线程安全的懒加载方案；④ 面试常考：单例实现方式、DCL 为什么要 volatile、枚举单例优势。

### 2.2 工厂方法模式（Factory Method）

**定义创建对象的接口，让子类决定实例化哪个类。**

```java
public interface Product { }
public class ProductA implements Product { }
public class ProductB implements Product { }

public interface Factory {
    Product createProduct();
}
public class FactoryA implements Factory {
    public Product createProduct() { return new ProductA(); }
}
```


> 🔍 **知识点深度解析**
>
> **作用**：定义创建对象的接口，让子类决定实例化哪个类，将对象创建与使用解耦。
>
> **原理**：工厂方法模式在简单工厂基础上进一步抽象：定义一个 Factory 接口声明工厂方法，由具体工厂子类决定创建哪种 Product。新增产品类型时只需新增对应工厂类，无需修改已有工厂代码，符合开闭原则。其本质是把 new 操作延迟到子类，客户端面向 Factory 接口编程而非具体类。
>
> **用法要点**：① 简单工厂违反开闭（新增产品需改工厂代码），工厂方法通过子类化解决  ② Spring 的 BeanFactory/FactoryBean 是工厂方法的典型应用  ③ 适合创建逻辑复杂、需要根据条件创建不同实现的场景  ④ 与策略模式区别：工厂关注创建，策略关注行为切换  ⑤ 面试常考：工厂方法与简单工厂区别、Spring 中的工厂模式应用

### 2.3 抽象工厂模式（Abstract Factory）

**创建一系列相关或相互依赖的对象家族。**

```java
public interface AbstractFactory {
    Button createButton();
    Text createText();
}
public class WindowsFactory implements AbstractFactory {
    public Button createButton() { return new WindowsButton(); }
    public Text createText() { return new WindowsText(); }
}
```


> 🔍 **知识点深度解析**
>
> **作用**：创建一系列相关或相互依赖的对象家族，保证产品族之间的兼容性。
>
> **原理**：抽象工厂定义多个工厂方法（createButton/createText），每个具体工厂生产一个产品族（如 Windows 族、Mac 族）的全套产品。客户端一次只使用一个具体工厂，确保同一族产品风格一致。新增产品族容易（加一个工厂类），但新增产品等级结构困难（需修改所有工厂接口及实现）。
>
> **用法要点**：① 产品族 vs 产品等级：族是同一品牌下不同产品，等级是不同品牌同一产品  ② 适合跨平台 UI、多数据库方言切换等成组对象创建场景  ③ 扩展性方向不对称：加产品族容易，加产品类型难  ④ Spring 的 AnnotationConfigApplicationContext 根据配置创建整套 Bean 可视为抽象工厂思想  ⑤ 面试常考：与工厂方法区别、产品族概念、开闭原则的倾斜性

### 2.4 建造者模式（Builder）

**将复杂对象的构建与表示分离，分步构建。**

```java
public class User {
    private String name;
    private int age;
    private String email;
    private User(Builder builder) {
        this.name = builder.name;
        this.age = builder.age;
        this.email = builder.email;
    }
    public static Builder builder() { return new Builder(); }
    public static class Builder {
        private String name;
        private int age;
        private String email;
        public Builder name(String name) { this.name = name; return this; }
        public Builder age(int age) { this.age = age; return this; }
        public Builder email(String email) { this.email = email; return this; }
        public User build() { return new User(this); }
    }
}
// 使用
User user = User.builder().name("Tom").age(20).email("tom@x.com").build();
```


> 🔍 **知识点深度解析**
>
> **作用**：将复杂对象的构建与表示分离，支持链式调用分步构建，解决多参数构造器问题。
>
> **原理**：Builder 模式在目标类中定义静态内部类 Builder，持有与目标类相同的字段，每个 setter 返回 this 实现链式调用，最终 build() 调用私有构造方法创建不可变对象。它把构造过程拆成清晰步骤，避免 telescoping constructor（重叠构造器），也比 JavaBean setter 方式更安全（对象可一次性构造为不可变）。Lombok @Builder 注解自动生成建造者代码。
>
> **用法要点**：① 链式调用 builder().name(x).age(y).build() 可读性极强  ② 适合参数多（≥4个）、部分参数可选的场景  ③ 构建与表示分离：相同构建过程可创建不同表示  ④ StringBuilder/StringBuffer 是建造者模式在 JDK 中的经典应用  ⑤ 面试常考：与工厂模式区别、Lombok @Builder 原理、不可变对象构建

### 2.5 原型模式（Prototype）

**通过复制现有对象创建新对象，避免复杂初始化。**

```java
public class Prototype implements Cloneable {
    private String name;
    @Override
    public Prototype clone() throws CloneNotSupportedException {
        return (Prototype) super.clone(); // 浅拷贝
    }
}
```

---


> 🔍 **知识点深度解析**
>
> **作用**：通过复制现有对象创建新对象，避免重复执行昂贵的初始化逻辑。
>
> **原理**：原型模式让对象实现 Cloneable 接口并重写 clone()，通过 super.clone() 进行位拷贝（浅拷贝）。Object.clone() 是 native 方法，直接在内存中复制二进制数据，不走构造方法，因此比 new 后逐字段赋值快。深拷贝需自行实现（序列化/反序列化或逐层 clone）。
>
> **用法要点**：① super.clone() 是浅拷贝，引用类型字段仍指向原对象  ② 深拷贝实现：实现 Cloneable 逐层 clone，或序列化反序列化  ③ 原型模式适合初始化成本高（如查库、加载配置）的对象  ④ Java 数组的 clone()、ArrayList.clone() 都是原型模式应用  ⑤ 面试常考：浅拷贝 vs 深拷贝、clone 为什么不走构造方法、Serializable 实现深拷贝


---
## 3. 结构型模式（7种）

### 3.1 代理模式（Proxy）

**为对象提供代理，控制对原对象的访问。**

```java
public interface Subject {
    void request();
}
public class RealSubject implements Subject {
    public void request() { /* 真实逻辑 */ }
}
public class Proxy implements Subject {
    private RealSubject real;
    public void request() {
        // 前置处理
        real.request();
        // 后置处理
    }
}
```

**动态代理**：JDK 动态代理（接口）、CGLIB（继承）。Spring AOP 基于动态代理。

> 🔍 **知识点深度解析**
>
> **作用**：在不修改原对象的前提下增强功能（日志、事务、权限、缓存）。
>
> **原理**：静态代理在编译期生成代理类，动态代理在运行时生成。JDK 动态代理通过 `Proxy.newProxyInstance` 生成实现目标接口的代理类，调用时走 `InvocationHandler.invoke`。CGLIB 通过 ASM 字节码框架生成目标类的子类，重写方法实现代理，不需要接口。Spring AOP 默认 JDK 动态代理（有接口时），无接口时用 CGLIB。
>
> **用法要点**：① JDK 代理需要接口，CGLIB 不需要（但不能代理 final 类/方法）；② Spring Boot 2.x 默认 CGLIB（spring.aop.proxy-target-class=true）；③ 同类内调用不触发代理（this 调用，需用 AopContext.currentProxy()）；④ 面试常考：JDK vs CGLIB、Spring AOP 原理、动态代理实现。

### 3.2 装饰器模式（Decorator）

**动态地给对象添加额外职责，比继承更灵活。**

```java
public interface Coffee {
    double cost();
    String desc();
}
public class SimpleCoffee implements Coffee {
    public double cost() { return 10; }
    public String desc() { return "咖啡"; }
}
public class MilkDecorator implements Coffee {
    private Coffee coffee;
    public MilkDecorator(Coffee coffee) { this.coffee = coffee; }
    public double cost() { return coffee.cost() + 2; }
    public String desc() { return coffee.desc() + "+牛奶"; }
}
// Java IO 流是装饰器经典应用：new BufferedInputStream(new FileInputStream(...))
```


> 🔍 **知识点深度解析**
>
> **作用**：动态地给对象添加额外职责，比继承更灵活，是组合优于继承的体现。
>
> **原理**：装饰器与被装饰对象实现同一接口，装饰器内部持有被装饰对象的引用，在调用委托前后增加增强逻辑。可以多层嵌套装饰，每层只关注一个增强点。装饰器模式在编译期不确定增强组合，运行时动态叠加；而继承是编译期静态确定的。Java IO 流体系（InputStream 嵌套 BufferedInputStream 嵌套 DataInputStream）是装饰器最经典的应用。
>
> **用法要点**：① 装饰器与被装饰者实现同一接口，持有被装饰者引用  ② 可多层嵌套，每层一个增强点，灵活组合  ③ 与代理模式区别：装饰器关注功能增强叠加，代理关注访问控制  ④ Java IO 流是装饰器模式教科书级案例  ⑤ 面试常考：装饰器 vs 代理 vs 继承、IO 流设计、多层嵌套顺序

### 3.3 适配器模式（Adapter）

**将不兼容的接口转换为客户端期望的接口。**

```java
public interface Target {
    void request();
}
public class Adaptee {
    public void specificRequest() {}
}
public class Adapter implements Target {
    private Adaptee adaptee;
    public void request() { adaptee.specificRequest(); }
}
```


> 🔍 **知识点深度解析**
>
> **作用**：将不兼容的接口转换为客户端期望的接口，让原本因接口不匹配而无法协作的类一起工作。
>
> **原理**：适配器模式分三种：类适配器（继承 Adaptee 实现 Target，Java 单继承限制大）、对象适配器（持有 Adaptee 引用实现 Target，推荐）、接口适配器（用抽象类默认实现接口所有方法，子类只重写需要的）。适配器在不修改原有代码的前提下做接口转换，符合开闭原则。Spring MVC 的 HandlerAdapter 就是典型的适配器模式，它让不同类型的 Controller 都能被统一调用。
>
> **用法要点**：① 对象适配器（组合）优于类适配器（继承），符合组合复用原则  ② 适合老系统对接、第三方库接口转换、统一不同实现  ③ Spring MVC HandlerAdapter 适配不同 Controller 类型  ④ 与装饰器区别：适配器改变接口，装饰器增强同接口功能  ⑤ 面试常考：适配器三种形式、HandlerAdapter 原理、何时使用适配器

### 3.4 桥接模式（Bridge）

**将抽象与实现分离，使两者可独立变化。**


> 🔍 **知识点深度解析**
>
> **作用**：将抽象与实现分离，使两者可独立变化，避免多层继承导致的类爆炸。
>
> **原理**：桥接模式把一个类的两个变化维度（如形状×颜色、消息类型×消息通道）拆成抽象层和实现层两个独立继承结构，抽象层持有实现层引用（桥接），两者通过组合连接而非继承绑定。新增一个维度的子类无需在另一个维度创建对应子类，类数量从 m×n 降为 m+n。JDBC 的 Driver 与 Connection 体系是桥接模式经典应用。
>
> **用法要点**：① 识别两个独立变化维度是使用桥接的前提  ② 组合代替多层继承，类数量从乘积变加和  ③ JDBC DriverManager 桥接不同数据库驱动  ④ 与适配器区别：桥接在设计时分离抽象与实现，适配器在运行时兼容已有接口  ⑤ 理解桥接关键：抽象层和实现层各自有继承树，中间用组合连接

### 3.5 外观模式（Facade）

**为子系统提供统一入口，简化调用。**


> 🔍 **知识点深度解析**
>
> **作用**：为子系统的一组接口提供统一入口，简化客户端调用，降低客户端与子系统的耦合。
>
> **原理**：外观模式在复杂子系统之上封装一个 Facade 类，内部编排多个子系统对象的调用顺序，客户端只需与 Facade 交互。外观不封装子系统功能，子系统仍可直接使用；外观只是提供一个更简单的入口。Spring 的 JdbcTemplate、String 类的 intern() 都体现了外观思想。
>
> **用法要点**：① 外观模式是迪米特法则（最少知识）的典型应用  ② 不阻止客户端直接使用子系统，只是提供更简单的选择  ③ 适合复杂子系统入口、分层架构中的层间封装  ④ SLF4J 对各日志框架的封装可视为外观模式  ⑤ 面试常考：外观 vs 中介者、外观在分层架构中的作用

### 3.6 组合模式（Composite）

**将对象组合成树形结构，统一处理单个和组合对象。**


> 🔍 **知识点深度解析**
>
> **作用**：将对象组合成树形结构表示部分-整体层次，使客户端对单个对象和组合对象的使用具有一致性。
>
> **原理**：组合模式定义统一 Component 接口声明叶子和容器的共同行为，Leaf 实现叶子节点行为，Composite 持有子 Component 集合并实现 add/remove/操作委托。客户端面向 Component 编程，无需区分叶子还是容器，递归调用天然处理树形结构。文件系统（文件/文件夹）、菜单树、组织架构是典型应用。
>
> **用法要点**：① 透明式（Component 声明 add/remove）vs 安全式（只有 Composite 声明）  ② 递归遍历是组合模式的核心操作  ③ 适合树形结构：菜单、目录、组织架构、DOM 树  ④ MyBatis 的 SqlNode（动态 SQL 节点）使用了组合模式  ⑤ 面试常考：组合模式结构、透明 vs 安全、递归遍历实现

### 3.7 享元模式（Flyweight）

**共享细粒度对象，减少内存占用。**（String 常量池、数据库连接池）

---


> 🔍 **知识点深度解析**
>
> **作用**：共享细粒度对象，减少内存占用，通过区分内部状态和外部状态实现复用。
>
> **原理**：享元模式把对象状态分为内部状态（不变、可共享，如字符的字体内码）和外部状态（变化、由客户端传入，如字符位置）。享元工厂用 HashMap 缓存已创建的享元对象，请求时先查缓存。String 常量池、Integer 缓存池（-128~127）、数据库连接池都是享元思想的应用。
>
> **用法要点**：① 内部状态共享不变，外部状态由客户端传入不共享  ② 享元工厂用 Map 缓存，类似对象池但重点是共享而非复用  ③ String 常量池、Integer.valueOf 缓存是 JDK 中的享元  ④ 线程池/连接池更接近对象池，享元侧重细粒度共享  ⑤ 面试常考：内部 vs 外部状态、String 常量池、Integer 缓存范围


---
## 4. 行为型模式（11种）

### 4.1 策略模式（Strategy）

**定义一系列算法，封装后可互换。**

```java
public interface PaymentStrategy {
    void pay(double amount);
}
public class AlipayStrategy implements PaymentStrategy {
    public void pay(double amount) { /* 支付宝支付 */ }
}
public class WechatPayStrategy implements PaymentStrategy {
    public void pay(double amount) { /* 微信支付 */ }
}
public class Order {
    private PaymentStrategy strategy;
    public void setStrategy(PaymentStrategy strategy) { this.strategy = strategy; }
    public void checkout(double amount) { strategy.pay(amount); }
}
```


> 🔍 **知识点深度解析**
>
> **作用**：定义一系列可互换的算法并分别封装，使算法可独立于使用它的客户端变化，消除大量 if-else。
>
> **原理**：策略模式定义 Strategy 接口声明算法方法，具体策略类各自实现，Context 持有当前 Strategy 引用并委托执行。运行时可动态切换策略。策略模式把算法选择与算法实现分离，客户端通过设置不同策略改变行为，比继承和条件分支更灵活。Spring 中把所有策略实现注入 Map 按 key 选择是企业开发常用写法。
>
> **用法要点**：① 策略模式核心用途：消除多重 if-else/switch 分支  ② Spring 中可注入 List<Strategy> 或 Map<String,Strategy> 动态选择  ③ 与工厂模式区别：策略关注运行时行为切换，工厂关注对象创建  ④ 与模板方法区别：策略用组合委托，模板用继承覆写  ⑤ 面试常考：策略模式消除 if-else、Spring 中策略注入、与工厂/状态模式区别

### 4.2 观察者模式（Observer）

**一对多依赖，状态变化时通知所有观察者。**

```java
public interface Observer {
    void update(String msg);
}
public class Subject {
    private List<Observer> observers = new ArrayList<>();
    public void attach(Observer o) { observers.add(o); }
    public void notifyAll(String msg) {
        observers.forEach(o -> o.update(msg));
    }
}
// Spring Event、Guava EventBus 是观察者模式应用
```


> 🔍 **知识点深度解析**
>
> **作用**：定义对象间一对多依赖，当一个对象状态变化时所有依赖者自动收到通知并更新，实现发布-订阅解耦。
>
> **原理**：Subject 维护 Observer 列表并提供 attach/detach/notify 方法，状态变化时遍历调用每个 Observer 的 update。推模型主动传数据，拉模型 Observer 自己从 Subject 获取。JDK 有 Observable/Observer（已废弃），Spring ApplicationEvent/EventListener、Guava EventBus 是生产级实现。观察者模式解耦了事件发布者和消费者，但注意通知顺序、异常处理和内存泄漏（忘记注销）。
>
> **用法要点**：① 推模型（Subject 推送数据）vs 拉模型（Observer 主动拉取）  ② Spring Event：ApplicationEvent + @EventListener 注解驱动  ③ 异步事件用 @Async，注意异常不会传播给发布者  ④ 注意注销观察者，防止内存泄漏  ⑤ 面试常考：观察者 vs 发布订阅、Spring Event 实现、同步异步事件

### 4.3 模板方法模式（Template Method）

**定义算法骨架，子类实现具体步骤。**

```java
public abstract class Game {
    public final void play() { // final 防止重写骨架
        initialize();
        startPlay();
        endPlay();
    }
    abstract void initialize();
    abstract void startPlay();
    abstract void endPlay();
}
```


> 🔍 **知识点深度解析**
>
> **作用**：在抽象类中定义算法骨架（固定步骤顺序），将某些步骤延迟到子类实现，骨架不可变而步骤可定制。
>
> **原理**：模板方法在抽象类中用 final 方法定义算法骨架（按固定顺序调用步骤方法），步骤方法分抽象方法（子类必须实现）、钩子方法（子类可选覆写）和具体方法（父类已实现）。子类通过覆写步骤定制行为但不能改变骨架顺序。这是好莱坞原则（Don't call us, we'll call you）的体现。Spring 的 JdbcTemplate、RedisTemplate、AbstractApplicationContext.refresh() 都是模板方法经典应用。
>
> **用法要点**：① 骨架方法用 final 修饰防止子类改变执行顺序  ② 钩子方法（hook）给子类可选的扩展点，返回 boolean 控制流程  ③ JdbcTemplate 把获取连接/Statement/关闭等固定步骤模板化，子类只处理结果映射  ④ 与策略模式区别：模板用继承（静态），策略用组合（动态）  ⑤ 面试常考：模板方法结构、JdbcTemplate 原理、钩子方法作用

### 4.4 责任链模式（Chain of Responsibility）

**请求沿链传递，直到有对象处理。**（Servlet Filter、Spring Interceptor）


> 🔍 **知识点深度解析**
>
> **作用**：将请求沿处理器链传递，每个处理器决定处理或传给下一个，避免请求发送者与接收者耦合。
>
> **原理**：每个 Handler 持有下一个 Handler 引用，handle 方法中若自己能处理则处理，否则调用 next.handle() 传递。链可在运行时动态组装。纯责任链（一个处理器处理或传递）与不纯责任链（每个处理器都可处理一部分，如 Filter）。Servlet Filter Chain、Spring Interceptor、Netty ChannelPipeline、MyBatis Plugin 都是责任链应用。
>
> **用法要点**：① Servlet FilterChain 是不纯责任链：每个 Filter 处理后继续 chain.doFilter  ② Spring Interceptor preHandle 返回 false 可中断链  ③ Netty ChannelPipeline 是双向链表结构的责任链  ④ 注意链尾兜底处理，避免请求静默丢失  ⑤ 面试常考：Filter vs Interceptor 区别、责任链组装方式、Netty Pipeline 原理

### 4.5 命令模式（Command）

**将请求封装为对象，支持撤销、排队。**（Runnable、Spring JdbcTemplate）


> 🔍 **知识点深度解析**
>
> **作用**：将请求封装为对象，从而可用不同请求参数化客户端、支持排队、撤销和日志记录。
>
> **原理**：命令模式把调用操作的对象（Invoker）与执行操作的对象（Receiver）解耦：Command 接口声明 execute()，具体命令持有 Receiver 并在 execute 中调用其方法。Invoker 只与 Command 交互，可把命令放入队列、记录日志或实现 undo。Runnable/Callable 是命令模式在 JDK 中的典型应用，Spring JdbcTemplate、线程池提交任务也体现了命令模式思想。
>
> **用法要点**：① Command 对象封装了接收者和动作，可排队、记录、撤销  ② Runnable/Callable 是最常见的命令模式接口  ③ 宏命令（MacroCommand）可组合多个命令批量执行  ④ 事务回滚、编辑器 Ctrl+Z 撤销是命令模式典型场景  ⑤ 面试常考：命令模式结构、Runnable 与命令模式、撤销实现

### 4.6 状态模式（State）

**对象内部状态改变时改变行为。**


> 🔍 **知识点深度解析**
>
> **作用**：对象内部状态改变时改变其行为，看起来像是改变了类，将状态判断逻辑分散到各状态类中。
>
> **原理**：状态模式把每个状态封装为独立 State 类，Context 持有当前 State 引用并委托给它处理请求，状态切换由 State 内部决定（设置 Context 的下一个状态）。它消除了大量 if-else 状态判断，新增状态只需加类。与策略模式结构几乎相同，但语义不同：策略由客户端选择可互换，状态由上下文自动切换、状态间有依赖流转关系。订单状态机、工作流引擎是典型应用。
>
> **用法要点**：① 状态模式与策略模式结构相同，语义不同：状态自动流转，策略外部选择  ② 状态切换逻辑放在 State 类内部（持有 Context 引用）  ③ 适合状态多（≥3个）且状态行为复杂的场景  ④ Spring StateMachine 是状态模式的框架级实现  ⑤ 面试常考：状态 vs 策略区别、状态机实现、消除状态 if-else

### 4.7 迭代器模式（Iterator）

**顺序访问集合元素，不暴露内部结构。**（Java Iterator）


> 🔍 **知识点深度解析**
>
> **作用**：顺序访问集合元素而不暴露集合内部结构，统一不同集合的遍历方式。
>
> **原理**：迭代器模式定义 Iterator 接口（hasNext/next/remove），具体集合实现自己的 Iterator（通常作为内部类），聚合对象提供 createIterator 工厂方法。客户端面向 Iterator 编程，无需关心底层是数组、链表还是树。Java 的 Iterator/Iterable、增强 for 循环都是迭代器模式应用。
>
> **用法要点**：① Iterable 接口的 iterator() 是工厂方法，返回 Iterator  ② 增强 for 循环要求对象实现 Iterable  ③ fail-fast 机制：modCount 检测并发修改抛 ConcurrentModificationException  ④ fail-safe：CopyOnWriteArrayList 迭代器使用快照不抛异常  ⑤ 面试常考：fail-fast vs fail-safe、Iterator vs ListIterator、Iterable 与 Iterator 区别

### 4.8 中介者模式（Mediator）

**用中介对象封装一组对象的交互。**（MVC 中的 Controller）


> 🔍 **知识点深度解析**
>
> **作用**：用中介对象封装一组对象的交互，使对象间不显式引用，降低多对多耦合。
>
> **原理**：中介者模式把多个 Colleague 之间的网状通信改为星型通信：所有 Colleague 只与 Mediator 通信，由 Mediator 协调转发。Colleague 之间不直接依赖，依赖关系集中到 Mediator。MVC 中 Controller 是 View 和 Model 的中介者，聊天室服务端是各客户端的中介者。但中介者容易膨胀为上帝对象。
>
> **用法要点**：① 将网状依赖改为星型依赖，Colleague 之间解耦  ② MVC Controller 是 View 与 Model 的中介者  ③ 中介者本身可能变得复杂（上帝对象风险），需注意职责边界  ④ 与外观模式区别：中介者封装同事间双向交互，外观单向简化子系统入口  ⑤ 适合多对象紧密耦合形成网状依赖的场景

### 4.9 备忘录模式（Memento）

**保存和恢复对象状态。**（数据库事务回滚、编辑器撤销）


> 🔍 **知识点深度解析**
>
> **作用**：在不破坏封装性的前提下捕获并保存对象内部状态，以便之后恢复，实现撤销/回滚。
>
> **原理**：备忘录模式有三个角色：Originator（原发器，创建备忘录并可从备忘录恢复）、Memento（备忘录，存储状态，对外部不可读）、Caretaker（管理者，持有备忘录但不修改内容）。Java 中可用序列化实现深拷贝备忘录。Spring Webflow 的 state、数据库事务回滚、编辑器撤销栈都是备忘录思想。
>
> **用法要点**：① 备忘录对外部封装状态细节，只有原发器能读写  ② 宽接口（原发器可访问全部状态）vs 窄接口（管理者只能传递）  ③ 序列化实现备忘录：ObjectOutputStream 深拷贝  ④ 注意备忘录内存开销，可只保存增量状态  ⑤ 应用：事务回滚、游戏存档、编辑器撤销、Spring Webflow

### 4.10 访问者模式（Visitor）

**在不改变元素类的前提下定义新操作。**


> 🔍 **知识点深度解析**
>
> **作用**：在不改变元素类的前提下定义作用于这些元素的新操作，将数据结构与操作解耦。
>
> **原理**：访问者模式利用双重分派（double dispatch）：Element 定义 accept(Visitor) 在方法中调用 visitor.visit(this)（this 是具体类型），Visitor 为每种 Element 定义 visit 方法。新增操作只需加 Visitor 实现类，无需改 Element；但新增 Element 类型需改所有 Visitor。适合元素结构稳定但操作频繁变化的场景，如编译器 AST 处理、ASM 字节码操作。
>
> **用法要点**：① 双重分派：accept 调 visit(this)，两次多态确定具体方法  ② 加操作容易（新 Visitor），加元素困难（改所有 Visitor）  ③ ASM ClassReader/ClassVisitor 是访问者模式经典应用  ④ 与注解处理器配合用于代码生成和静态分析  ⑤ 面试常考：双重分派原理、访问者适用场景、ASM 中的 Visitor

### 4.11 解释器模式（Interpreter）

**定义语言文法，解释句子。**（SQL 解析、正则表达式）

---


> 🔍 **知识点深度解析**
>
> **作用**：定义语言的文法表示并构建解释器来解释语言中的句子，适合简单文法解析。
>
> **原理**：解释器模式将文法规则表示为抽象表达式类层次：TerminalExpression（终结符，如变量/常量）和 NonterminalExpression（非终结符，如加减运算），通过组合表达式构建抽象语法树（AST），context 存储全局信息。正则表达式 Pattern/Matcher、SQL 解析器、SpEL 表达式、规则引擎都使用了解释器模式思想。实际开发中一般用表达式引擎库（Aviator、QLExpress）而非手写。
>
> **用法要点**：① 终结符表达式（变量/常量）vs 非终结符表达式（运算/逻辑）  ② 通过组合模式构建 AST，递归解释执行  ③ Pattern/Matcher、SpEL、Spring ExpressionParser 是应用案例  ④ 复杂文法维护困难，生产中优先用 Aviator/QLExpress/Drools  ⑤ 面试常考：解释器模式结构、正则表达式引擎、表达式语言


---
## 5. 面试高频考点

1. **单例模式**：DCL、volatile、枚举单例
2. **代理模式**：JDK vs CGLIB、Spring AOP
3. **装饰器模式**：与代理区别、Java IO
4. **策略模式**：与工厂模式区别、消除 if-else
5. **观察者模式**：Spring Event、发布订阅
6. **模板方法**：与策略模式区别
7. **责任链模式**：Filter/Interceptor 实现
8. **建造者模式**：链式调用、与工厂区别
9. **适配器模式**：类适配器 vs 对象适配器
10. **六大原则**：SOLID 理解和应用

---


---
## 📝 精简总结

- 创建型：单例、工厂方法、抽象工厂、建造者、原型 — 解决对象创建问题
- 结构型：代理、装饰器、适配器、桥接、外观、组合、享元 — 解决类/对象组合问题
- 行为型：策略、观察者、模板方法、责任链、命令等 11 种 — 解决对象交互问题
- 核心原则：开闭原则（对扩展开放）、依赖倒置（面向接口编程）
- 实际开发中最常用：单例、工厂、建造者、代理、策略、观察者、模板方法

---

[[02-后端开发/MOC-后端开发|← 返回后端开发 MOC]] | [[Home|🏠 返回首页]]
