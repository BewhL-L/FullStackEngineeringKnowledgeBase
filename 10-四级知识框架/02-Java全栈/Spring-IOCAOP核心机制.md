---
title: Spring IoC 与 AOP 核心机制
tags: [#Java全栈/Spring, 原子笔记, 待完善]
created: 2026-08-13
updated: 2026-08-13
status: 🟡 骨架已填充
source: 
---

# Spring IoC 与 AOP 核心机制

> Spring 框架两大基石

**所属板块**：[[MOC-Java全栈-四级展开.md|Java全栈]]
**标签**：`#Java全栈/Spring`
**学习状态**：🟡 骨架已填充（待深入完善）

---

## 📋 四级子知识点清单

| 序号 | 子知识点 | 章节锚点 | 独立笔记 | 状态 |
|------|----------|----------|----------|------|
| 1 | Bean 生命周期 | [1](#1-Bean-生命周期) | ⬜ 待写 | 🔴 未开始 |
| 2 | 依赖注入方式与循环依赖 | [2](#2-依赖注入方式与循环依赖) | ⬜ 待写 | 🔴 未开始 |
| 3 | Bean 作用域 | [3](#3-Bean-作用域) | ⬜ 待写 | 🔴 未开始 |
| 4 | AOP 代理机制（JDK/CGLIB） | [4](#4-AOP-代理机制（JDK/CGLIB）) | ⬜ 待写 | 🔴 未开始 |
| 5 | 切面通知类型与执行顺序 | [5](#5-切面通知类型与执行顺序) | ⬜ 待写 | 🔴 未开始 |
| 6 | 事务传播机制 | [6](#6-事务传播机制) | ⬜ 待写 | 🔴 未开始 |

---

## 📖 核心内容

### 1. Bean 生命周期

Bean 生命周期是工程实践中总结出的最佳实践，旨在解决特定场景下的共性问题。正确应用可以显著提升开发效率和系统质量。

**核心要点**：
- 基本概念与定义：理解Bean 生命周期的核心含义和解决的问题
- 工作原理与机制：掌握底层实现逻辑和关键流程
- 适用场景与边界：明确什么时候使用、什么时候不适合
- 最佳实践与注意事项：总结实际使用中的经验和坑点
- 与相关技术的对比：理解差异化优势和选型依据


**常见问题**：
Q: Bean 生命周期和相关技术有什么区别？
A: 核心区别在于设计目标和适用场景。Bean 生命周期更侧重于特定场景下的优化，而相关技术可能有更广泛的适用性。

---
---

### 2. 依赖注入方式与循环依赖

依赖注入是一种设计模式，将对象的创建和使用分离，由外部容器负责注入依赖。FastAPI 通过 Depends() 实现强大的依赖注入系统。

**核心要点**：
- Depends() 声明依赖：FastAPI 自动解析并注入
- 依赖嵌套：依赖可以依赖其他依赖，形成依赖树
- 全局依赖：通过 app.dependencies 应用到所有路由
- 依赖作用域：默认每个请求创建一次，可使用 yield 实现资源清理
- 类依赖：可注入类实例，支持 __init__ 参数自动解析

**代码示例**：
from fastapi import FastAPI, Depends, HTTPException

app = FastAPI()

# 函数依赖
async def get_db():
    db = DatabaseSession()
    try:
        yield db
    finally:
        db.close()

# 类依赖
class CurrentUser:
    def __init__(self, token: str = Header(...)):
        self.user = verify_token(token)
        if not self.user:
            raise HTTPException(401)

@app.get("/items/")
async def list_items(
    db = Depends(get_db),
    user: CurrentUser = Depends(),
):
    return db.query(Item).filter(owner=user.id).all()

**常见问题**：
Q: 依赖注入和直接 import 有什么区别？
A: 依赖注入便于测试（可 mock 依赖）、复用和生命周期管理，直接 import 是硬编码耦合。

---
---

### 3. Bean 作用域

Bean 作用域是现代软件开发中常用的技术手段，通过特定的设计思路实现更高效、更可靠的系统功能。在实际项目中需要根据具体需求合理应用。

**核心要点**：
- 基本概念与定义：理解Bean 作用域的核心含义和解决的问题
- 工作原理与机制：掌握底层实现逻辑和关键流程
- 适用场景与边界：明确什么时候使用、什么时候不适合
- 最佳实践与注意事项：总结实际使用中的经验和坑点
- 与相关技术的对比：理解差异化优势和选型依据


**常见问题**：
Q: Bean 作用域和相关技术有什么区别？
A: 核心区别在于设计目标和适用场景。Bean 作用域更侧重于特定场景下的优化，而相关技术可能有更广泛的适用性。

---
---

### 4. AOP 代理机制（JDK/CGLIB）

AOP 代理机制（JDK/CGLIB）是工程实践中总结出的最佳实践，旨在解决特定场景下的共性问题。正确应用可以显著提升开发效率和系统质量。

**核心要点**：
- 核心思想：AOP 代理机制（JDK/CGLIB）的设计理念和解决的根本问题
- 关键组件：系统的主要组成部分及其职责
- 工作流程：从输入到输出的完整处理链路
- 性能特点：时间/空间复杂度和资源消耗
- 演进历史：技术发展脉络和版本差异


**常见问题**：
Q: 学习AOP 代理机制（JDK/CGLIB）有哪些常见误区？
A: 常见误区包括只记概念不理解原理、不区分适用场景盲目使用、忽略性能和可维护性权衡。

---
---

### 5. 切面通知类型与执行顺序

切面通知类型与执行顺序是工程实践中总结出的最佳实践，旨在解决特定场景下的共性问题。正确应用可以显著提升开发效率和系统质量。

**核心要点**：
- 基本概念与定义：理解切面通知类型与执行顺序的核心含义和解决的问题
- 工作原理与机制：掌握底层实现逻辑和关键流程
- 适用场景与边界：明确什么时候使用、什么时候不适合
- 最佳实践与注意事项：总结实际使用中的经验和坑点
- 与相关技术的对比：理解差异化优势和选型依据

**代码示例**：
# 切面通知类型与执行顺序 示例
# 基础用法
# TODO: 根据具体知识点补充代码示例

**常见问题**：
Q: 切面通知类型与执行顺序和相关技术有什么区别？
A: 核心区别在于设计目标和适用场景。切面通知类型与执行顺序更侧重于特定场景下的优化，而相关技术可能有更广泛的适用性。

---
---

### 6. 事务传播机制

事务传播机制涉及系统设计的核心思想，关注如何在复杂环境中实现目标功能。掌握该知识点有助于深入理解技术栈的底层原理。

**核心要点**：
- 核心思想：事务传播机制的设计理念和解决的根本问题
- 关键组件：系统的主要组成部分及其职责
- 工作流程：从输入到输出的完整处理链路
- 性能特点：时间/空间复杂度和资源消耗
- 演进历史：技术发展脉络和版本差异


**常见问题**：
Q: 学习事务传播机制有哪些常见误区？
A: 常见误区包括只记概念不理解原理、不区分适用场景盲目使用、忽略性能和可维护性权衡。

---
---


---

## 🔗 关联知识

### 前置依赖
- 无特殊前置要求

### 后续延伸
- 

### 跨板块关联
- 待补充

### 相关笔记
- 

---

## 💡 实践要点

- 

---

## ❓ 常见问题

1. **Q**: 
   **A**: 

---

## 📚 参考资料

- 

---

## 📝 学习日志

| 日期 | 学习内容 | 状态 |
|------|----------|------|
| 2026-08-13 | 创建笔记骨架 | 🔴 未开始 |
| 2026-08-13 | 批量填充四级子知识点内容 | 🟡 骨架已填充 |

---

[[MOC-Java全栈-四级展开.md|← 返回Java全栈 MOC]] | [[10-四级知识框架/00-总控/四级框架总索引|🗺️ 返回四级框架总索引]] | [[Home|🏠 返回首页]]
